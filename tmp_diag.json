{
  "csharpCode": "using UnityEngine; using UnityEngine.UI; using System.Linq; using System.Text; public class Script { public static string Main() { var allGos = Resources.FindObjectsOfTypeAll<GameObject>(); var sb = new StringBuilder(); string[] btns = new string[] { \"DriverButton\", \"GolfinButton\", \"SpinButton\" }; foreach (var btnName in btns) { var go = allGos.FirstOrDefault(g => g.name == btnName && g.scene.IsValid()); if (go == null) { sb.AppendLine(btnName + \": NOT FOUND\"); continue; } sb.AppendLine(\"=== \" + btnName + \" ===\"); var rootCg = go.GetComponent<CanvasGroup>(); sb.AppendLine(\"  RootCG: \" + (rootCg != null ? \"YES a=\" + rootCg.alpha : \"NONE\")); foreach (var img in go.GetComponentsInChildren<Image>(true)) { sb.AppendLine(\"  Img[\" + img.gameObject.name + \"] spr=\" + (img.sprite != null ? img.sprite.name : \"null\") + \" color.a=\" + img.color.a); } foreach (var cg in go.GetComponentsInChildren<CanvasGroup>(true)) { sb.AppendLine(\"  CG[\" + cg.gameObject.name + \"] a=\" + cg.alpha); } } return sb.ToString(); } }"
}
