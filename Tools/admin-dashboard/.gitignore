# dependencies
node_modules/

# next.js build output
.next/
out/

# env — never commit secrets
.env.local
.env*.local

# misc
*.tsbuildinfo
next-env.d.ts
.DS_Store
npm-debug.log*

# Cloudflare / OpenNext build output
.open-next
.wrangler
cloudflare-env.d.ts
.dev.vars

# cf-deploy.sh stages the env file here mid-build. Ignored so the script cannot
# make the tree look dirty to its own version stamp (PIPELINE_HARDENING §23).
.env.development.local.deploy-stash
