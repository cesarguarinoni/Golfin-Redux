---
name: Push changes to GitHub
description: User wants all changes pushed to GitHub after making them, not just committed locally
type: feedback
---

Always push changes to GitHub after committing them. Don't just commit locally.

**Why:** User explicitly requested this workflow so they can pull changes in Unity without waiting.

**How to apply:** After every `git commit`, follow up with `git push`.
