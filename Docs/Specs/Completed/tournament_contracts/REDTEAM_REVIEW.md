# Red-Team Review — `tournament_contracts` (T1)

**Reviewer:** golfin-redteam-reviewer (adversarial gate)
**Timestamp:** 2026-06-25 10:30 CEST
**Verdict:** ARCHITECT_REVIEW_PASS
**Task class:** CONTRACTS-ONLY C# (no UI / no Figma / no scene / no mesh — visual gates legitimately N/A)

I tried to break this and could not. Every gate below was re-generated by me from the
live loaded assembly and the working tree — none carried forward from the reviewer.

---

## Independent compile + test run (the thing both prior reviewers SKIPPED)

Both `SELF_REVIEW.md` and `ARCHITECT_REVIEW.md` explicitly admit they did NOT re-run
`tests-run`. I did. Via `unity-mcp-cli` against the live Editor (PID 6219):

**Compile gate — re-derived from the loaded AppDomain assembly (`script-execute`):**
```
EditorUtility.scriptCompilationFailed: False
EditorApplication.isCompiling:         False
Golfin.Tournaments assembly present:   True   (loaded, 16 types)
Golfin.Tournaments.Tests present:      True
Golfin.UI.Rankings.Core present:       True
Golfin.Save present:                   True
Tournaments assemblyReferences:        Golfin.UI.Rankings.Core, Golfin.Save  (+ auto UnityEngine.UI)
ITournamentBackend method count:       8
HoleResult.RngSeed: True   HoleResult.InputLog: True
TournamentState values: 6   EntryStatus values: 4
StubTournamentBackend implements ITournamentBackend: True
```
`script-execute` only runs if `Assembly-CSharp` compiled clean — so the project compiles
end-to-end, not just the Tournaments assembly. No compile errors anywhere.

**EditMode test run — FULL suite (`tests-run`, EditMode):**
```
Status: Passed | Total 521 | Passed 518 | Failed 0 | Skipped 3 | Duration ~41s
```
The 3 skips are pre-existing `Golfin.Physics.Tests.HoleCompleteDriverTests` (Stage C1
no-ops) — unrelated to this task. Zero failures across the entire project.

**Positive confirmation the 14 Tournament tests ARE present and ran** (reflected the
compiled `Golfin.Tournaments.Tests.TournamentContractsTests` fixture — the runner summary
only echoes non-passing entries, so absence had to be ruled out directly):
```
[Test] methods discovered in compiled fixture: 14
BotCard_ConstructAndRead, BotFieldConfig_ConstructAndRead, EntryState_ConstructAndRead,
EntryStatus_AllCasesCovered, HoleResult_CarriesRngSeedAndInputLog,
HoleResult_NullInputLog_BecomesEmptyList, PrizeTable_ConstructAndRead,
ShotCommand_ConstructAndRead, StubBackend_ImplementsInterface,
TimeProviderClock_WrapsITimeProvider, TournamentDefinition_ConstructAndRead,
TournamentLeaderboardEntry_ConstructAndRead, TournamentResult_ConstructAndRead,
TournamentState_AllCasesCovered
```
Exactly 14, names matching the report. They are inside the 518 passed. **Real, not fabricated.**

---

## Leaf boundary — attacked, holds

- `Golfin.Tournaments.asmdef` references = `["Golfin.UI.Rankings.Core", "Golfin.Save"]` (verbatim).
- `grep "Golfin.Tournaments"` across ALL `.asmdef` + `using` in `Assets/Scripts` → **zero inbound
  production references** (only the Tournaments tree + its own Tests). True leaf.
- `Golfin.UI.Rankings.Core.asmdef`: `references: []`, `noEngineReferences: true`; folder holds
  only `ITimeProvider.cs`, `ILeaderboardProvider.cs`, `LeaderboardPeriodKey.cs` (interfaces/value
  types, zero MonoBehaviour). Leaf-to-leaf, no UI controller dep.
- `Golfin.Save.asmdef`: `references: []` (only Newtonsoft precompiled). Clean leaf.
- **Cycle check:** neither `Golfin.Save` nor `Golfin.UI.Rankings.Core` back-references
  `Golfin.Tournaments` (grep empty). No cycle possible.

## Leaked logic — attacked, none found

Grep sweep over the 12 production `.cs` (Tests excluded): `MonoBehaviour`, `Resources.`,
`TextAsset`, `System.IO`/`File.`/`StreamReader`, `Mathf.`/`OrderBy`/`.Sort(`, `PlayerPrefs`/
`SaveDataHost`/`.Save(`, prize/score arithmetic (`+=` etc.) — **all empty**. Only executable
code is DTO ctors, one-line null→empty-list guards, the `TimeProviderClock.UtcNow => _provider.UtcNow`
passthrough (SPEC §3-mandated), and the stub returning `static readonly` fixtures.
`DateTime.UtcNow` appears only in `StubTournamentBackend.cs` ×3 (canned fixture data, SPEC §5
permits "fixed data") and as `<c>` doc-markup in `ITournamentClock.cs`. Confined to the stub,
never in a DTO or the clock.

## DTO shape vs GDD §8 / SPEC §2 — field-by-field, no miss

Verified against `Docs/Game Design/Tournaments_GDD.md` §8 (the 8 seam signatures match
verbatim, param order/count correct) and SPEC §2. `HoleResult` carries `rngSeed`+`inputLog`;
`TournamentLeaderboardEntry` is a `struct` with `Strokes` (not `Score`) + `Thru` + all 11 fields
mirroring `Golfin.UI.Rankings.LeaderboardEntry`; `TournamentState` = exactly the 6
(Upcoming/Open/Playing/Ending/Closed/Ended); `EntryStatus` = exactly the 4
(NotEntered/InProgress/Finished/DNF). `GetMyEntry`/`GetResults` nullable returns are the correct
C# shape for the spec's own "returns null if…" prose. No deviation that breaks a future save schema.

## Reuse mandate — no duplication

- `BotCard.BotId` is a `string` whose XML-doc cites `fake_players.csv` ("Do not define new players
  here"); `fake_players.csv` exists (`id,username,characterId,level`: fp_001/FRODO…). No roster type.
- `ITournamentClock`/`TimeProviderClock` wraps the EXISTING `ITimeProvider`; project-wide grep shows
  NO new `TimeProvider` impl created. Single time source preserved.
- `BotFieldConfig.BracketWeights` keys cite `bot_difficulty.csv` (exists).
- `TournamentLeaderboardEntry` mirrors the existing `LeaderboardEntry` shape.

## §7 decisions recorded for T4

D-Tie indivisible-item rule on BOTH `PrizeBand` and `TournamentResult` XML-doc; `holeSet` =
explicit `IReadOnlyList<string>`; `inputLog` = minimal `ShotCommand` struct; time-seam = option (b)
(pre-existing Core split). All present.

## Working-tree integrity (Rule 13)

`git status --porcelain` = only the Tournaments tree + the task spec folder. Every `.cs`/`.asmdef`
paired with its `.meta`; the `Tests` folder meta is correct. Zero Physics edits, zero scene edits,
zero `M_Splash*.mat` edits, zero `*Gate` scenarios. HEARTBEAT has the `=== iter-1 kickoff baseline`
block. No `CESAR_REJECTION.md` (clean first iteration → Step 1 replay has nothing to replay).

---

## Three break-attempts (required) — each failed

1. **Visual/capture:** N/A by design. Contracts-only; canonical PNG is an intentional idle
   1170×2532 Game View (verified real, not flat-fabricated). No video, no capture mechanism, no
   bespoke `*Gate` scenario (Scenarios.cs untouched). Nothing visual can be wrong → no FAIL here.
2. **Numeric/threshold (code analogue):** Every objective number re-derived from the live loaded
   assembly + full test run. 16 types / 8 methods / 6+4 enum values / RngSeed+InputLog present /
   518 pass-0 fail / 14 named tests. All match the report exactly. No disagreement → fails to break.
3. **Spec-intent:** T1's point is to freeze a downstream-stable shape with no future save break.
   Anti-cheat slots carved in, stable `ShotCommand`, D-Tie rule where T4 reads it, leaf with zero
   inbound deps (so the T4 `LocalTournamentBackend` swap can't break callers), reuse seams pointed
   at existing infra. Letter AND intent satisfied → fails to break.

## Non-gating notes (surfaced for downstream, do NOT block)

- Report checklist line "All 13 `.cs` files begin with `#nullable enable`" is **inaccurate**: there
  are 12 production `.cs` (the 13th item is the asmdef), and `TournamentEnums.cs` +
  `TournamentLeaderboardEntry.cs` omit the directive. Neither uses nullable-reference annotations
  where it would change semantics, so it is cosmetic, not a contract failure — but the blanket claim
  should be tightened on a future polish pass. (Overstatement, not a fabricated tool-output → not a
  Rule-6 fabrication.)
- Stub fixture uses `BotId/"frodo"` etc. — the real ids are `fp_001`; the field is just a `string`,
  so this is a placeholder value, harmless for a compile-gate stub.
- `StubTournamentBackend` `DateTime.UtcNow` ×3 in fixtures — could pass an `ITournamentClock` for
  max hygiene; not a T1 concern.

These do not affect contract correctness and do not gate.

---

## Verdict

Genuinely tried to break it via independent compile, independent full-suite test run, live
reflection, leaked-logic grep, GDD §8 authority check, cycle check, reuse check, and working-tree
audit — every attack failed. STATUS → `ARCHITECT_REVIEW_PASS`.

| File | Why |
|---|---|
| `/Users/cesar/Documents/GolfinRedux/Docs/Specs/Active/tournament_contracts/REDTEAM_REVIEW.md` | this verdict |
| `/Users/cesar/Documents/GolfinRedux/Docs/Specs/Active/tournament_contracts/STATUS.md` | flipped to ARCHITECT_REVIEW_PASS |
