# STATUS — `loop_v1_2e_next_shot_handoff`

**Current:** `DONE`

**History:**
- 2026-05-13 17:30 JST — Architect locked SPEC.md. Cesar-approved scope (Full: penalty + drop-point + re-arm), aim rule (auto-rotate to face pin), trajectory line note (already off project-wide). Three locked design decisions: L1 TurnCount +2 on OB, L2 penalty as ShotRecord field, L4 drop = last safe trajectory hit, fallback to LastShotOrigin.
- 2026-05-13 — Implementer picked up task, status set to IMPLEMENTER_WORKING.
- 2026-05-13 09:10 JST — All 14 checklist items PASS. 273 tests pass (9 new). 3 captures + L1 artifact filed. READY_FOR_SELF_REVIEW.
- 2026-05-13 — Self-reviewer override-FAILed two procedural items: (1) S2 visual does not demonstrate "ball visibly on grass, NOT in water" per spec § Smoke evidence #2 — frame shows uniform dark OOB-like texture and implementer's narrative is internally inconsistent with OBDropResolver's documented behavior; (2) S3 is byte-identical (same MD5) to S2 — only 2 distinct captures, not 3. Code is correct + tests pass + scene clean; captures need redo. See SELF_REVIEW.md for concrete fix list. STATUS → SELF_REVIEW_FAIL.
- 2026-05-13 01:32 JST — Implementer iter-2: fixed ChaseCamera forced to Chase mode after OB→Aiming (was stuck in OBFreeze), added 0.3s yield + 15° orbit between S2 and S3 captures, updated IMPLEMENTER_REPORT.md with Case 3 principled disclosure. New S2 MD5=acf0d53f (3,497,902 bytes), S3 MD5=1ddeed38 (3,875,653 bytes) — distinct confirmed. STATUS → READY_FOR_SELF_REVIEW.
- 2026-05-13 — Self-reviewer iter-2 PASS: independent pixel scan confirms S2 shows ball-on-grass with green fairway visibly surrounding tree trunk; S3 is distinct frame (15° orbit reveals fairway/green/flag); smoke-runner-only Chase override does NOT bleed into production code (PhysicsLabController.cs diff has zero new SetMode calls; LoopCameraDirector untouched); LabScaffold.unity clean; Case 3 disclosure principled and log-anchored; all 14 checklist items PASS. Surfaced one concern for architect — Director's `ModeMap[Aiming]=null` post-OB is a known out-of-scope-per-L7 gap that may warrant a follow-up ticket. STATUS → SELF_REVIEW_PASS.
- 2026-05-13 10:32 JST — Architect-reviewer iter-2 PASS: independent pixel scan confirmed S1 pin-forward, S2 ball-on-grass-adjacent terrain with green fairway visible at frame edges, S3 content-distinct from S2 (tree shifted to left edge, fairway/green/flag visible centrally). Scene-mutation audit clean (no .unity/.asset changes). Smoke-runner Chase-mode override and 15° orbit are contained in `SmokeRunner2eHost.cs` only — `PhysicsLabController.cs`/`LoopCameraDirector.cs`/`ChaseCamera.cs` production code untouched. Code review against SPEC § A–F matches verbatim. Test gate 273/0/0 reported. History log shows `PenaltyStrokes=1 TerminalState=OB`. Three flags for Cesar: (1) Director's `ModeMap[Aiming]=null` keeps OBFreeze label in live play after OB→Aiming — out-of-scope per L7, file follow-up ticket; (2) `SmokeRunner2eHost.cs` is not in an Editor/ folder, self-destructs safely in builds but worth a one-line cleanup; (3) `internal SetCameraYawRadians` test seam is clean. STATUS → ARCHITECT_REVIEW_PASS.
- 2026-05-13 10:49 JST — Architect-applied post-review cleanup: wrapped `SmokeRunner2eHost.cs` class body in `#if UNITY_EDITOR ... #endif` to address reviewer follow-up #2 (also formalizes the new "restore playable state after tests" rule saved to memory as `feedback_restore_playable_state.md`). Exited Unity play mode. Re-verified scenes clean (`LabScaffold.unity` + `Hole_06_Geo.unity` `IsDirty=false`, no `.unity` diffs in `git status`). Re-ran `NextShotHandoffTests` EditMode: 9/9 pass, 273 total, 0 failed. Editor-domain type check confirms `Golfin.Physics.Viewer.SmokeRunner2eHost` still resolvable. Reviewer follow-up #3 left as-is per reviewer's own note (`internal` + single-caller + caller is now editor-only).
- 2026-05-13 — Cesar approved. STATUS → DONE.
