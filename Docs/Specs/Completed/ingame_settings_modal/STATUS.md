DONE

Cesar-approved 2026-08-18 ("We are done"). Green thumbnail descoped by his call the
same day ("No green thumbnail needed. Same images used for hole selection are fine")
— slot and resolution code removed, PLAYING card now carries only the hole-select
image.

14/14 acceptance criteria PASS, no open blockers. Verified on an authenticated
session through the FULL real player path (Home -> PRACTICE PLAY -> Hole Selection ->
hole card PLAY -> matchmaking -> gameplay), every interaction via the real widget's
own onClick.

Deliverables:
  - videos/ingame_settings_modal_demo.mp4  (1170x2532, 48.3s, captioned)
    also at Docs/Reports/Media/ingame_settings_modal_demo.mp4
  - screenshots/01..05

Nice-to-have follow-ups (not blockers, no spec owed):
  - Device pass for audible volume, real touch through the raycaster, and QUIT
    hidden in a live tournament round.
  - Abandoned-round handling for tournaments/1v1 (killing the app mid-round) is
    still unspecified — flagged in SPEC.md's out-of-scope section.
