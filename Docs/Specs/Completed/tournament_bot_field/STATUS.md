DONE

# STATUS — tournament_bot_field (T3)

## ✅ DONE — Cesar approved 2026-06-26
Full pipeline cleared (impl → self-review → review → red-team), TWO red-team passes (iter-1 FAILed for a dead bracket-mix invariant; iter-2 fixed it with an internal test seam + negative control + B1/B2/B3 fixes). Final gate `ARCHITECT_REVIEW_PASS` (iter-2 red-team mutation-walked the production seam). Live suite **50/50 PASS**. Folder moved Active → Completed. T3 code committed scoped (`Assets/Scripts/Tournaments/**`); ShellScene/T7 working-tree drift deliberately left untouched (not T3).

### Open items carried to T4 (do not lose)
- **BotCount > 120 — needs a decision before T4 ships fields > 120.** Roster `fake_players.csv` = 120 rows; fields are no-repeat ⇒ `|field| ≤ 120`. Either extend `fake_players.csv` to ≥ max BotCount, or cap `BotCount ≤ 120` in `tournaments.csv`. (This is why the bracket-mix test runs at N=600 aggregate, not the SPEC's literal 500 single field.)
- **`BracketWeights` = distribution INTENT, not a per-tournament quota.** A small (24-bot) single-seed field can swing ±~10pp/bracket as normal multinomial variance — by design, not a bug. Set design expectations accordingly.
- **(Optional) tighten the bracket-mix gate.** The 8pp tolerance + uniform negative control catches gross weight-ignoring regressions but lets subtle ~5pp dilution slip. Fine for T3's 12-line sampler; revisit if T4 adds more complex weighting.
- **T4 reuse note:** pass the same `TextAsset.text` to `FakePlayerRosterParser` to avoid a duplicate `Resources.Load`; `Project` is the pure `(card, now)` fn `GetLeaderboard` calls, then filter to `complete == true` (D4).

## ✅ Decisions locked (Cesar, 2026-06-25)
- **D0 — PRNG:** ship our own explicit ~15-line PCG/xorshift seeded by the stable hash (NOT System.Random).
- **D1 — strokes distribution:** use the §3 proposed per-bracket meanΔ/stdev values, shipped in tunable `bot_score_brackets.csv`.
- **D2 — identity selection:** honor `BracketWeights` — sample target bracket from weights (seeded), pick a no-repeat roster identity in that tier (nearest-bracket fallback if exhausted); distribution params come from the slot's bracket.
- **D3 — worst-hole cap:** **par+4**, reusing `versusStrokeCapOverPar`. `clamp(strokes, 1, par+4)`. Stroke-bounds test asserts `1 ≤ strokes ≤ par+4`.
- **D4 — provisional ranking:** **ranking shows only FINISHED bots.** T3 `Project` still emits `(thru, revealedStrokes, complete)` unchanged; T4's `GetLeaderboard` filters to `complete == true` (hide any bot with `thru < H`). T3 needs no change for this — just ensure `complete` is correct.

## Staging
- [x] **Stage 1** — stable hash (FNV/xxHash) + explicit PRNG + per-bot seed streams + hash-vector/determinism tests.
- [x] **Stage 2** — `bot_score_brackets.csv` + bracket→strokes roll + identity selection + `RollField` + tests.
- [x] **Stage 3** — pace schedule + `Project` + pace/projection/trickle tests.

## Review
- 2026-06-25: SELF_REVIEW_PASS (golfin-self-reviewer). Verdict FORWARD_TO_ARCHITECT — all SPEC §7 invariants mapped to real assertions, D0–D4 implemented as spec'd, N=60 bracket-mix deviation flagged but legitimate roster-pool constraint. See `SELF_REVIEW.md`.
- 2026-06-25: READY_FOR_REDTEAM (golfin-reviewer). PASS — independently re-derived FNV-1a hash of "abc" to confirm the pinned constant (`-3547061803046329763`) is mathematically real; line-by-line invariant→assertion map confirms every SPEC §7 row is backed by ≥1 real assertion with zero tautologies; D0–D4 honored exactly (slot-bracket distribution params verified at L229 + L240). N=60 deviation accepted as documented constraint with explicit T4 follow-up (extend roster vs cap BotCount). Hands to red-team for adversarial gate. See `ARCHITECT_REVIEW.md`.
- 2026-06-25: ARCHITECT_REVIEW_FAIL (golfin-redteam-reviewer). Bracket-mix invariant is dead (proved by uniform-random substitution). B1/B2/B3 secondary. See `ARCHITECT_REVIEW.md` § RED-TEAM REVIEW.
- 2026-06-25: READY_FOR_SELF_REVIEW (iter-2). Fixed bracket-mix seam (internal overload + InternalsVisibleTo); 5-seed aggregate test (600 draws); negative control; B1 4-phase pace; B2 drop else-break; B3 CSV drift guard. 50/50 tests PASS.
- 2026-06-25: SELF_REVIEW_PASS (iter-2, golfin-self-reviewer). Verdict FORWARD_TO_ARCHITECT — bracket-mix blocker fixed correctly (sampled-target distribution via internal `out` seam, not roster level). Negative control discrimination verified independently in Python (uniform sampler produces 8.67pp Δ for bracket "50", 0.67pp above 8pp tolerance — reliable trigger). T1 BotCard contract untouched (zero diff). Headless integrity preserved (no UnityEngine imports in math/generator). B1/B2/B3 fixed + tested. Live 50/50 PASS re-confirmed. Non-blocking flags: (a) 8pp tolerance only catches ≥80%-uniform samplers; (b) single-seed variance is normal multinomial noise (Mersenne baseline confirms), not generator clustering; (c) authors should treat `BracketWeights` as distribution intent not per-tournament quota. See `SELF_REVIEW.md`.
- 2026-06-25: READY_FOR_REDTEAM (iter-2, golfin-reviewer). PASS — re-ran the suite myself (`unity-mcp-cli run-tool tests-run`, EditMode, namespace `Golfin.Tournaments.Tests` → 50/50 PASS). Independently re-derived the negative control's discrimination math in Python on the published seed IDs (bracket "50" Δ=8.67pp > 8pp tolerance, 5.5σ; correct sampler maxΔ=2.5pp with 5.5pp headroom). Verified the test routes through the internal `out` seam (BotFieldGenerator L242 captures `SampleBracket` output, test L552 aggregates it directly), T1 contract untouched (zero diff on BotFieldConfig.cs), headless integrity preserved (zero `using UnityEngine`). B1/B2/B3 fixes verified in source + tests. Rule 13 attribution honest (ShellScene = pre-existing T7, 0 diff vs iter-1 commit). Adjudicated the two self-reviewer flags: (a) 8pp+uniform-control discrimination accepted as the gate (full-uniform fails reliably; subtler-bias regressions are not in T3's plausible failure space, and tightening would risk false positives against legitimate multinomial noise — but flagged for red-team to re-attack); (b) concur with multinomial-noise framing — `BracketWeights` is distribution intent not per-tournament quota, recorded as T4 design note. Hands to red-team adversarial gate. See `ARCHITECT_REVIEW.md` § "ARCHITECT REVIEW — tournament_bot_field (T3) — Iter-2".
- 2026-06-25: ARCHITECT_REVIEW_PASS (iter-2, golfin-redteam-reviewer, SECOND pass). Genuinely attacked the iter-1 blocker and could not revive it. **MUTATION-WALKED the production seam in Python:** replacing the real `SampleBracket` with uniform inside the RollField path makes `RollField_BracketMix_SampledTargetsApproximateWeights` FAIL at 8.67pp — the test reads `SampleBracket`'s real `out` output (L242→L286→L548), not a re-computation; the fix is real, not cosmetic. **Negative-control teeth confirmed:** inlined seam uses the identical seed (`StableHash(seedId:bracket)`) and identical draw count (1 ulong/bot), isolating only the sampler fn — bracket "50" trips at 8.67pp (5.5σ). **Empty bracket-1 fallback:** end-to-end ported the default field — 3 bracket-1 targets are sampled (~10%, weights honored), all route through the nearest-bracket fallback, 24 unique valid IDs resolve; `NoDuplicates`+`AllInFakePlayers` cover the path. **B1 re-fuzzed below 2H:** the 4-phase pace algorithm holds both invariants at EVERY window down to 1s (0.11×H) — compress-first means the +1s repair never overruns; my iter-1 break is gone. **B2/B3 verified** (Project O(H) unconditional; CSV-drift guard loaded the real shipped 120-row file, SkippedTests=0). **Integrity:** live 50/50 PASS re-run; pinned hash bit-exact; implementer's single-seed 30.8% claim verified real (Rule 6 clean, no fabrication); BotCard contract 0-diff; ShellScene = uncommitted pre-existing T7 drift (zero `m_IsActive:0`, all RectTransform overrides) — Rule-12 close-out item for Cesar, not T3. **Residual (non-blocking, documented):** 8pp tolerance slips subtle weight-dilution bugs (50%-uniform=6.67pp, swap-two-weights=5.33pp, `<=`-vs-`<`=2.50pp all PASS); catastrophic bugs (index-shift=16.5pp, full-uniform=8.67pp) are caught. Not a likely T3 regression vector (`SampleBracket` is a 12-line draw with no dilution knob), so not a FAIL — surfaced for T4 tightening. See `ARCHITECT_REVIEW.md` § "RED-TEAM REVIEW (iter-2)". STATUS → ARCHITECT_REVIEW_PASS; hands to Cesar for final approval.
