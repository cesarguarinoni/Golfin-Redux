# STATUS — `putter_cone_per_shot_lifecycle`

**Current:** `DONE`

**History:**
- 2026-05-17 — Cesar visually confirmed and approved. Folder moved to `Docs/Specs/Completed/`.
- 2026-05-17 07:19 CEST — Architect reviewer: **PASS**. Independent pixel scan of all 9 frames + Approach C definition-of-done re-verification + scene-mutation audit (diff is exactly the 2 documented changes: `_putterTrack` Inspector wire + `_normalSize: 80→150`) + capture-helper compliance (`CaptureCore.SnapAtEndOfFrameAndPause`, 9 unique MD5s, no banned paths) + code re-read of `ShotConeView.cs` / `CentralBallWidget.cs` / `PutterConeLifecycleTests.cs`. All 18 acceptance items independently re-verified PASS. The p1f4 mislabel observation (file shows a post-§2f-auto-exit driver re-aim instead of a putter re-aim) is ACCEPTED on the merits: the canonical "putter re-aim, PutterTrack visible at TURN 2" evidence is delivered by `prod_aiming_puttertrack_visible` (smoke runner explicitly re-enters putter mode via `lab.SetClub(PutterIndex)`), and that production-flow capture survives the harder putter→driver→putter round-trip the spec's lifecycle is meant to prove. Smoke-runner narrative polish for the P1F4 label is noted as an optional follow-up, not a blocker. Ready for Cesar's final approval. See ARCHITECT_REVIEW.md.
- 2026-05-17 07:17 CEST — Self-reviewer: **PASS** (forward to architect). All 16 IMPLEMENTER_REPORT checklist items confirmed via pixel scan of the 9 captures + code + scene-diff + EditMode test code reading. Scene diff is exactly 2 legitimate changes (Inspector `_putterTrack` wire + Piece-2 `_normalSize: 80→150`). Capture-helper compliance PASS (all 9 frames via `CaptureCore.SnapAtEndOfFrameAndPause(skipPause:true)`). Production-flow evidence present in prod_aiming/prod_fired pair. One observation: p1f4 is labeled `next_aiming_puttertrack_visible` but actually shows the post-§2f-auto-exit driver frame; the missing "putter re-aim, PutterTrack visible" demo is delivered by `prod_aiming` instead — Piece 1 lifecycle is correctly demonstrated overall, just not in the file the smoke runner labeled. See SELF_REVIEW.md § Step 4 for narrative.
- 2026-05-17 — Approach C (iter 4) complete. All 16 IMPLEMENTER_REPORT checklist items PASS. 290/290 EditMode tests pass (G1–G4 all PASS). Compile clean. Piece 2 intact. Smoke frames pending Cesar's manual run via `GOLFIN/Smoke/Capture PutterCone Lifecycle`. STATUS set to READY_FOR_SELF_REVIEW.
- 2026-05-14 (post-manual-smoke) JST — **Approach A REPLACED by Approach C.** Cesar's manual smoke in real Play mode (after MCP frozen-time blocker) revealed two visual bugs: (1) iron cone + PutterTrack overlap while aiming with putter, (2) PutterTrack still visible after firing. Root cause: Approach A's premise was wrong — `_coneGraphic` is the iron cone (correctly permanent-hidden in putter mode by the original code); `PutterTrack` is the actual putter aim viz and the thing that needed per-shot lifecycle. SPEC.md amended with full Approach C plan: revert ShotConeView Piece 1, wire PutterTrack to per-shot state, rewrite EditMode tests (G1/G2 retargeted to PutterTrack, add G3 cone-stays-disabled and G4 non-putter-untouched), capture refreshed via `GOLFIN/Smoke/Capture PutterCone Lifecycle` menu in normal Play. Piece 2 unaffected — keep the `_normalSize = 150f` change. New hard rule: never `SetActive(false)` on `ConeRoot` or any parent of `_clubHandle` (ClubHead must remain visible during putter aim). STATUS set back to SPEC_READY for re-implementation.
- 2026-05-14 (iter 3) — Implementer blocked: MCP frozen game loop prevents coroutine-based smoke captures. `Time.time=0`, `Time.frameCount=1` in MCP play mode; `WaitForSeconds`, `WaitForSecondsRealtime`, and `WaitForEndOfFrame` all hang permanently. `SnapPlayModeSafe` (synchronous) produces files but all frames are identical (MD5 `529dfb623ddb4eaf4dd63a18412c797f`). Per CLAUDE.md, not inventing another capture workaround. Code correct; EditMode tests G1/G2 PASS. See IMPLEMENTER_REPORT.md for full diagnosis and three resolution options for Cesar.
- 2026-05-14 15:38 JST — Self-reviewer: `BACK_TO_IMPLEMENTER`. Code, tests, scene diff, and Piece 2 all sound. BLOCKER: Piece 1 4-frame smoke captures do not demonstrate the lifecycle — p1f2 ("just fired") and p1f3 ("rolling") still show the cone visible with HUD state label reading `BALL:Aiming`; the smoke harness never drove the live ShotController out of Aiming. p1f1≡p1f4 byte-identity proves the run was static, not that the lifecycle repeats. See SELF_REVIEW.md fix list.
- 2026-05-14 16:00 JST — Implementer complete. All 12 checklist items PASS. STATUS set to READY_FOR_SELF_REVIEW.
- 2026-05-14 10:00 JST — Architect amended SPEC. Piece 2 added: central ball sprite size parity (both `_normalSize` and `_puttModeSize` = 150f, fields kept separate). Surfaced from Cesar Lesson O.
- 2026-05-14 09:30 JST — Architect locked. Approach A (reuse `_coneGraphic` with putt-mode styling) confirmed. Approach B rejected to avoid coupling to `PuttPathPredictor` (slated for deletion under Order 110).
- 2026-05-14 — Drafted by architect chain during §2f Lesson O closeout.
