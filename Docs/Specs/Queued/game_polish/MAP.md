# `game_polish` — per-screen motion map (APPROVED by Cesar 2026-09-03 — decisions inline in § Open questions)

Architect, 2026-09-03. Notion **2111**. Runs AFTER `design_consistency_audit` (2112) and its approved Quick fixes — we do not animate inconsistencies. Approve / strike / edit each row; the spec(s) are written from the approved version and the folder moves to `Docs/Specs/Active/game_polish*/`.

## What we already have (from `gps_polish`, DONE `5506d2c67`)

`UiMotion` (`Assets/Scripts/UI/Polish/UiMotion.cs`): `Fade` 0.15 s · `Rise` 0.25 s / 16 px · `Pop` 0.20 s / `Unpop` · `Slide` 0.25 s · `CountUp` 0.40 s · `Bump` 1.06 / 0.10 s · `Stagger` 30 ms cap 12 · `Pulse` 0.6 s · `Tween` · `Then`; unscaled time, interruption-safe (`Run(host, ref handle, …)`), `UiMotion.Enabled`. `UiSelection` (chip bump + two-Image cross-fade), `KeyboardInset`, `PendingSpend`, `ShimmerBlock` prefab (`Assets/Prefabs/UI/Gps/ShimmerBlock.prefab` — GPS folder; the game gets its own copy or the prefab moves to `Assets/Prefabs/UI/Common/`, see G6), `ModalController.animateShow` (opt-in pop; default false, pinned by test), `GpsScreenTransition` (GPS-only layered push, one branch in `ScreenManager.Navigate:315`). The gates: motion-invariants JSON, 0 px rest parity, captioned videos, isolated per-frame GC ≤ 32 B.

**Rules baked in (Cesar):** fade-to-black stays for cross-pillar moves; a layered push only INSIDE a pillar when the background does not change; no haptics; `UiMotion` public API changes need a `Docs/GPS/GPS_BACKLOG.md` row; never touch `Assets/Scripts/UI/Gps/*`.

## Where the backgrounds actually are (measured from the YAML — this decides every push)

| Pillar | Screens | Background sprite (GUID) | Verdict |
|---|---|---|---|
| Play | `ModeSelectionScreen`, `HoleSelectionScreen`, `MissionSelectionScreen`, `TournamentHoleSelectionScreen` | **all `2e5476ee…`** | same → **push** |
| Play | `TournamentSelectionScreen`, `TournamentLeaderboardScreen`, `RankingsScreen` (Leaderboard) | **all `0d425c0a…`** | same among themselves → **push** |
| Play | ModeSelection → TournamentSelection; TournamentSelection → TournamentHoleSelection; TournamentHoleSelection → TournamentLeaderboard; HoleSelection → Leaderboard | `2e54…` ↔ `0d42…` | background CHANGES → **push with chrome cross-fade (option (b), SHIPPED in `game_polish_a`)** |
| Gacha | `GeneralShopScreen`, `GachaHistoryScreen`, `GachaPrizesScreen` | **all `5ec22d10…`** | same → **push** |
| Characters | `RosterScreen` (no Background child — the stage renders behind it), `StaminaShopSelectionScreen`, `StaminaShopDetailScreen` (background inside a nested prefab, not readable from YAML) | ? | **fade stays** unless Code measures the same backdrop |
| Inventory | `InventoryScreen` only (`BG` `44d64d73…` + `Rim`) | — | no screen-to-screen move; tabs cross-fade (see row) |
| Home | `HomeScreen` (`c230d900…`) | — | every move out of Home is cross-pillar → **fade stays** (incl. Home → GpsHub, Home → Leaderboard) |

## G · Global items (apply everywhere; approve as a set)

| # | Item | Proposal | Needs your call? |
|---|---|---|---|
| G1 | **Layered push for the game** | Generalise, don't fork: a screen-agnostic `LayeredPush` in `Assets/Scripts/UI/Polish/` with `GpsScreenTransition`'s shape (chrome `Background`/`BG` + persistent bars cross-fade in place, only the content layer slides, `PushDur`, parallax 0.3, one-at-a-time, `CompleteActiveNow`, `SkipEntry`). `ScreenManager.Navigate` gets ONE more branch: same pillar (`PillarOf(a)==PillarOf(b)`), both roots have a chrome/content split (`Background`|`BG` + `Content`|`ContentArea`|`CardsContainer`|`GameScreenContent` — the split names differ per screen; a per-screen map, not a rename), backgrounds identical → push. Direction: `push` → Forward, `GoBack` → Back, `NavigateToPillar` (nav-bar tap) → stays fade (it is a pillar reset). GPS code untouched; if `GpsScreenTransition` and `LayeredPush` end up sharing code, the shared part is new, the GPS class only delegates (GPS_BACKLOG row). | **Option for background-changing moves inside Play:** (a) fade-to-black, your rule as stated; (b) push with the chrome cross-fade — exactly what GPS does, it just becomes visible when the two backgrounds differ. I recommend (a) for now and a 5-second video of (b) on ModeSelection → TournamentSelection so you can judge it. |
| G2 | **Screen entry `Rise`** | Content layer rises 16 px / 0.25 s on fade-path arrivals (boundary entries, pillar resets); skipped after a push (already animated). Every shell screen. | no |
| G3 | **Modals pop** | Set `animateShow = true` on the game modals (prefab/scene flag, no code default change — the pinned test keeps default false): `LevelUpModal`, `ClubLevelUpModal`, `BagClubModal`, `BagSelectionModal`, `ItemUseModal`, `TournamentSignupModal`, `MatchMakingModal`, `GachaRatesModal`, `StartingCharacterConfirmModal`, `InGameSettingsModal`, `HoleCompleteModal`, `TournamentResultModal`. Retire the legacy `useAnimation` alpha-only `FadeIn`/`FadeOut` for those (the pop includes the alpha). | yes — all of them, or keep the result modals (HoleComplete / TournamentResult) on their own choreography (row M) |
| G4 | **The three retrofits** | `VersusResultModalController.PopInScaleRoutine` → `UiMotion.Pop` (same 0.9→1.0 / 0.2 s, so no visible change — rest-parity + a frame-by-frame diff prove it). `DailyMissionPillController.SlideRoutine` + glow → `UiMotion.Slide` + `UiMotion.Pulse` (the glow curve is already the `Pulse` shape by the GPS spec's own description). `GachaRevealModalController` `StepEnter` (ease-out-back 0.6→1) / `StepPop` (ease-out-back 0.25→1 + position) / `StepShake` → `Pop`/`Tween`; ease-out-back is NOT in `UiMotion` today → add an `Ease` parameter (API change → GPS_BACKLOG row) or keep the overshoot curves local and only route the plain parts. | yes — API change vs local curves |
| G5 | **RP count-up on GAME deltas** | `PersistentUIManager.ArmRewardPointsCountUp()` exists (GPS-only arm, 5 s). Arm it from: shop purchase confirm, stamina purchase, gacha pull, mission claim, hole-complete rewards, tournament result, level-up spend. The label counts instead of snapping. Same for the stamina figure if the top bar shows one (check `PersistentUIManager`). | no |
| G6 | **Shimmer + fade-in on cold fetches** | Move `ShimmerBlock.prefab` to `Assets/Prefabs/UI/Common/` (GPS builder path updated = one-line GPS touch → tell the GPS session; or duplicate — I prefer the move) and place hosts at: Rankings list + Top-3, TournamentSelection cards, TournamentLeaderboard rows, GachaHistory rows, GeneralShop cards (content catalog), MissionSelection progress. Cache-hit path skips it, error shows the existing empty/error label with a `Fade`. | move vs duplicate |
| G7 | **`PendingSpend` audit** | Already wired: LevelUp, StaminaShopDetail, GeneralShop, ClubLevelUp, TournamentSignup, ModeCard. Audit the rest of the network CTAs (gacha PULL x1/x10 — the reveal modal covers the round trip, so probably only the pre-modal tap; mission CLAIM on HoleComplete; Settings sign-out; anything that awaits `Golfin.Net`) and wire what is missing; table in the report, as gps_polish A7. | no |
| G8 | **Stagger on first paint** | List rows / grid cells `Stagger`-rise on a FETCH paint, never on a cache repaint (the `PaintGate` idea from `GpsPaintMotion` — game copy in `Polish/`, GPS file untouched). Sites per screen below. | no |
| G9 | **Selection states** | `UiSelection` bump + cross-fade on: Inventory tabs, Rankings tabs/filters, GachaHistory `FiltersIconRow`, Settings language buttons, Mode cards' selected state, hole cards. | no |
| G11 | **Bottom-nav selected state** (added by Cesar 2026-09-03: *"solid cyan looks ugly"*) | Today `PersistentUIManager.UpdateScreenHighlight` tints the whole baked button sprite (`Home.png` etc. — disc + ring + glyph in one PNG) `iconActiveColor = Color.cyan`; `GpsNavBarHighlight` reads the same two colours. Figma has NO selected variant (`New Nav Bar Buttons` is `Property 1=Default` only), so the treatment is ours. **DECIDED: gold halo behind the selected disc + brighter ring, glyph stays white, cross-faded between slots** — one shared `NavSlotHighlight` component used by BOTH the game bar and the GPS bar (Cesar: "this should also be done in GPS when changed" → the one authorised GPS-file touch). Lands in `game_polish_a` §D7. | decided |
| G10 | **Gates** (same as gps_polish) | invariants JSON per transition (`fail = 0`), rest parity 0 px on every screen, captioned videos per pillar, isolated GC ≤ 32 B/frame, `ButtonPressFeedback` on every Button touched (Rule 11), no GPS path in `git status`. | no |

## Per screen

Columns: what moves today → what is proposed. "—" = nothing proposed.

| Screen | Transition (in / out) | Entry | Lists / cards | Modals | Live values | Loading | Notes / risks |
|---|---|---|---|---|---|---|---|
| **Home** (`HomeScreen`) | fade everywhere (all moves are cross-pillar) → **unchanged** | `Rise` on the content (notice panel, promo, mode carousel, pills) after boot/return | notice carousel: page change cross-fade + `PageDots` bump (today: snap); mode carousel keeps `ModeCarouselController.LerpToTargetLayout` (already eased) | — | RP / stamina in the Top UI (G5) | promo banner + notice from the server: `Fade` in on paint, no shimmer (they have art) | `DailyMissionPill` retrofit (G4). `GpsPill` is GPS-owned — leave it. Home is the highest-traffic screen: rest parity here is the gate you will look at first. |
| **Mode Select** (`ModeSelectionScreen` + `ModeCard`) | from Home: fade (cross-pillar) → unchanged; → HoleSelection / MissionSelection: **push Forward**; → TournamentSelection: **G1 option**; back: push Back | `Rise` on `CardsContainer` | cards `Stagger`-rise on entry (3–4 cards, local data — the ONE place a stagger on a non-fetch paint is worth it, because it is the pillar's front door; say no if you want the rule kept pure) | signup / entry-denied modals: pop (G3) | — | — | `ModeCardController` already uses `PendingSpend` on PLAY. `TournamentTempEntry` child — confirm it is dead before animating around it. |
| **Hole Select** (`HoleSelectionScreen`, `HoleCard`) + **Tournament Hole Select** | from ModeSelection: push; → Leaderboard: G1 option (background changes); → gameplay: the scene load, out of scope | `Rise` on `Content` | hole cards stagger on first paint; selected card `Bump` | — | — | tournament variant: rows from the server → shimmer (G6) | `TournamentHoleCard_Locked/Finished/Next` states: state change = cross-fade, not snap. |
| **Missions** (`MissionSelectionScreen`, `MissionCard`) | from ModeSelection: push | `Rise` | cards stagger on fetch paint; progress bars `Tween` old→new width (Filled → width-driven only if the audit's fix list changed them first — else `fillAmount` tween) | claim confirmation (if any) pop | mission counters `CountUp` | server progress → shimmer | `RankingsButton` is inactive today — nothing to animate. |
| **Tournaments** (`TournamentSelectionScreen`, cards; `TournamentLeaderboardScreen`; `TournamentSignupModal`; `TournamentResultModal`) | Selection ⇄ Leaderboard: **push** (same BG); Selection ⇄ HoleSelection: G1 option | `Rise` | selection cards stagger + `Fade` on fetch; leaderboard rows stagger; sticky player row `Rise` in | signup modal pop (G3); result modal: row M | leaderboard rank / score `CountUp` after a result | shimmer on both lists | `TournamentCloseButton` / `BackButton` (inactive) — real widgets only (Rule 2). |
| **Rankings** (`RankingsScreen` = `ScreenId.Leaderboard`) | reached from Home / HoleSelection via `LeaderboardButton` — background changes from both → fade, or G1 option; rides the history stack (no pillar) | `Rise` | Top-3 cards `Pop` in order (1→2→3, or 3→2→1 as a reveal — your pick), list rows stagger; tab/filter change cross-fade (G9) | — | user card rank/points `CountUp` | shimmer (G6) | `RankingsDemoRecorder` exists — reuse for the video. |
| **Inventory / Bag** (`InventoryScreen`: CLUBS / BAGS / BALLS / ITEMS tabs) | single screen; from nav bar: fade (pillar reset) | `Rise` on `ContentArea` | tab switch: cross-fade panels + indicator `Slide` (today `SetActive` snap in `InventoryScreenController.ShowTab`); carousels keep their own scroll; card grids stagger on tab first-show only | `BagClubModal`, `BagSelectionModal`, `ItemUseModal`, `ClubLevelUpModal`: pop (G3); club level-up: stat bars `Tween` + numbers `CountUp` (mirror of the Roster level-up) | — | — | Inventory has the most LiberationSans sites (27) — audit fixes land first. `Rim` layer is chrome, stays put. |
| **Roster / Characters** (`RosterScreen`, `LevelUpModal`) | Roster → StaminaShop: fade unless Code proves the same backdrop | `Rise` on `DetailPanel` only (the carousel is the character stage — leave it) | character switch: detail panel cross-fade (today: snap on carousel settle?) — Code confirms current behaviour first | level-up modal pop (G3); SP allocation: stat bars `Tween`, stat numbers `CountUp`, level `Pop` | — | — | `StarterInstructionBar` (starter mode) `Rise` in/out. |
| **Shop** (`GeneralShopScreen`, `GeneralShopCard`; `StaminaShopSelection/Detail`) | GeneralShop ⇄ GachaHistory ⇄ GachaPrizes: **push**; Stamina: fade (see Characters) | `Rise` | shop cards stagger + `Fade` on catalog paint; `HistoryChip` bump on tap | purchase confirm (if modal) pop | balance `CountUp` after purchase (G5); card price/quantity flip cross-fade after a stock change | shimmer on catalog (G6) | `PendingSpend` already on BUY. `GeneralShopDemoRecorder` exists. |
| **Gacha** (`GachaHistoryScreen`, `GachaPrizesScreen`, `GachaRevealModal`, `GachaRatesModal`) | pushes inside the pillar (above) | `Rise` | history rows stagger; filters cross-fade (G9); prizes grid stagger | reveal: retrofit (G4) — the choreography (waiting → shake → pops by rarity) stays exactly as it is, only the primitives change; rates modal pop (G3) | ticket count `CountUp` after a pull | history shimmer | The reveal is the most-loved motion in the game; the retrofit gate is a frame-by-frame diff, not "looks the same". |
| **Settings** (`SettingsScreen` overlay, `SettingsController.OpenSettings`, submenus) | overlay: `Background` scrim `Fade` + `SettingsPanel` `Pop` (today: `SetActive` snap); close reverses | — | submenu open/close: cross-fade + `Rise` (Sounds, Language, About…); language switch: the whole shell re-labels — a `Fade` over the relabel hides the pop | confirm dialogs pop | sliders (sound) — no motion | — | `InGameSettingsModal` (gameplay) is a `ModalController` → G3; keep both Settings surfaces in step. |
| **Result modals** (row M): `HoleCompleteModal`, `VersusResultModal`, `TournamentResultModal` | — | pop in (G3 / retrofit) | reward rows `Stagger`-rise; RP/points `CountUp`; rank `Pop`; mission-complete banner `Rise` + `Pulse` once | — | (these ARE the live values) | — | These carry the game's biggest numbers; keep the count-ups honest — `CountUp` snaps to the exact final value (pinned). |
| **PersistentUI** (Top UI + nav bar) | stays static during pushes (the chrome invariant, A5 seam test) | — | nav slot selection: **new gold-halo selected state (G11), cross-faded** — replaces the cyan tint on both the game bar and the GPS bar | — | RP / stamina count-ups (G5) | — | Shared with GPS — every change here is measured on a GPS screen too. |
| **Toast** | — | keeps `ToastController.Fade`; optionally route through `UiMotion.Fade` (zero visible change) | — | — | — | — | low value; include only if a retrofit sweep is wanted for uniformity. |

## Proposed slicing (so each task is one reviewable video set)

- **`game_polish_a` — navigation & structure (DONE 2026-09-04, `b2496871d`; option (b) shipped; §D7 nav selected state shipped on both bars):** G1 (`LayeredPush` + the branch + per-screen split map), G2, Inventory tabs + Settings overlay/submenus + Rankings/Gacha filters cross-fades, G9, G10 gates. Videos: one per pillar walk.
- **`game_polish_b` — content & modals:** G3, G4 (three retrofits, frame-diff gate), G5, G6, G7, G8, the result-modal choreography, Roster/Inventory level-up bars. Videos: one per modal family + one per list screen.
- (optional) **`game_polish_c` — sweep:** scroll feel parity with Inventory's values on every ScrollRect, safe-area check on the screens `safe_area_top_bar` did not cover, `ButtonPressFeedback` coverage table, Rubik Medium sites list for the game (pairs with P-012).

## Open questions for you (answer inline or in chat)

1. ~~G1 option~~ **DECIDED 2026-09-03: (a) + a video of (b). REVISED during `game_polish_a` (Cesar, 2026-09-04): option (b) SHIPPED** — every in-pillar move pushes, the chrome cross-fades when the backdrop differs (16 ordered pairs moved from fade to push; invariants `measured 87 / fail 0 / seam cover 1.0`). The `AllowBackgroundCrossFade` flag is gone. Fade-to-black remains ONLY for cross-pillar moves (Home ↔ anything, nav-bar pillar resets, Login/Loading/GPS boundaries).
2. ~~G3~~ **DECIDED: pop ALL 12 game modals**; HoleComplete / TournamentResult keep their inner choreography on top of the pop.
3. ~~G4~~ **DECIDED: add an optional `Ease` parameter to `UiMotion`** (default = today's ease-out cubic, GPS behaviour unchanged; `Docs/GPS/GPS_BACKLOG.md` row added).
4. ~~G6~~ **DECIDED: move `ShimmerBlock.prefab` to `Assets/Prefabs/UI/Common/`** (GpsPolishBuilder path constant = the one GPS-folder line; GPS session informed via GPS_BACKLOG row).
5. Mode Select front-door stagger — **default YES** unless Cesar strikes it.
6. Rankings Top-3 reveal — **default 3 → 2 → 1** (podium reveal) unless Cesar strikes it.
7. ~~Slicing~~ **DECIDED: three tasks — `game_polish_a` (navigation & structure), `game_polish_b` (content & modals), `game_polish_c` (sweep).**
