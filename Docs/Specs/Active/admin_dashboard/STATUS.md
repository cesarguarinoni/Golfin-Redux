# STATUS — admin_dashboard

- **2026-08-13 — ▶ IN_PROGRESS (hold lifted). v1 + v2 BUILT by the Architect/Cowork session (not Claude Code — the old TellCode kickoff is superseded); imported to `Tools/admin-dashboard/` on the PC (`f5594562f` v1, `91e466684` v2).** Delivered well past the original §5 steps 1–2: scaffold (Next.js App Router + TS + Tailwind, panel registry, server-only service_role client via the `server-only` package, ADMIN_EMAILS allowlist enforced on every route, PRODUCTION vs MOCK banners), **Users panel** (list, search, provider/unconfirmed/banned filters, stat cards, detail drawer with RP + activity/gift split, identities, ledger + activities tabs), **all §5 step-3 mutations** (username edit → profiles + user_metadata, resend confirmation, password reset, manual confirm, ban/unban, delete with typed-email double-confirm + cascade warning), **RP grant/adjust** (the welcome-grant replacement: + via `earn_pts_v2`, − via `spend_pts` incl. the `insufficient` branch, required reason, audited), plus **Points panel** (global ledger viewer) and **Audit Log panel**. Every mutation writes `admin_audit_log` with before/after snapshots. Mock-data mode mirrors the 5 real accounts so the whole UI is testable without secrets.
- **2026-08-13 — both pre-prod flags CLEARED (Architect, via Supabase SQL editor):** (1) `migrations/2026_08_13_admin_audit_log.sql` **APPLIED to prod** — verified table exists, RLS enabled, `authenticated` SELECT = false (so the Audit panel is now backed by a real table, not silently empty); (2) RPC signatures **verified live** against `pg_get_function_arguments` — `earn_pts_v2(p_user_id uuid, p_action text, p_pts integer, p_description text, p_key uuid)` / `spend_pts(p_user_id uuid, p_amount integer, p_reason text, p_key uuid)`, exactly what `lib/mutations.ts` calls; the stale "assumed" caveat is deleted from the README and the migration header is stamped APPLIED.
- **REMAINING for live cutover (Cesar):** paste `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` into `Tools/admin-dashboard/.env.local` (never committed; `.gitignore`d), then `npm install && npm run dev`. ⚠️ Note: in mock mode login accepts any credentials, so "it let me in" is NOT evidence live auth works — the first real sign-in (email/password; use the gmail/Cratilo account, since Google OAuth is not wired into the dashboard yet) is itself the auth verification. First live Adjust RP closes the loop: dashboard → `earn_pts_v2` → ledger the game spends from.
- **Known v1 gaps (deliberate, not defects):** admin sign-in is email/password only (no Google OAuth — the wonderwall account can't sign in yet; allowlist covers both Cesar addresses); no hosting/deploy (local `npm run dev`); v2/v3 panels from spec §4 (characters, clubs, progress, moderation, tournaments, IAP) still pending Track B game-state sync.

- **2026-08-12 (later) — ⏸ ON HOLD per Cesar:** Reward Points move to the backend first (`Docs/Specs/Active/reward_points_backend/`). Resumes after that lands — the v1.5 points panel will then read the unified ledger (spec open question 2 answered: BEFORE). The §5 steps 1–2 kickoff in TellCode.md stays valid as written; do not start it until the hold lifts.
- **2026-08-12 — SPEC_READY.** Spec approved in concept by Cesar (2026-08-11 decision of record; plan approved 2026-08-12). Kickoff issued 2026-08-12 for §5 steps 1–2 (scaffold + Users panel read-only). Pointer + kickoff archived in `Docs/TellCode.md`.
- Next state: `IN_PROGRESS` when the hold lifts and Claude Code starts step 1.
