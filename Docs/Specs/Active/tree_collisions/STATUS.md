ARCHITECT_DECISION_ITER8

# STATUS — `tree_collisions`

**State:** ARCHITECT_DECISION_ITER8 — iter-7 FINDINGS adjudicated (test heuristic flawed, sim correct); route back to implementer (iter-8)
**Notion:** Order 348 (P2, Gameplay Polish) · Phase 2 (tree-aware bot) = Order 351
**Spec:** `Docs/Specs/Active/tree_collisions/SPEC.md`
**Prepared:** 2026-06-11 (Architect; design Cesar-approved same day)

## History
- 2026-06-11 — Investigation: three tree placement paths (TreePlacer terrain trees, TreePlacer StandaloneTrees GOs, TreeBrushTool PaintedTrees mixed), unified via per-hole bake into the deterministic sim (ball is not a Unity rigidbody — colliders irrelevant). SPEC written. Locked: damping-only canopy, trunk hard reflect, all sim phases, auto re-bake on scene save, bot tree-awareness deferred to Order 351, dormant packs untouched. Awaiting Cesar kickoff.
- 2026-06-12 — IMPLEMENTER_WORKING: implementer activated, reading spec, building tree collision system.
- 2026-06-12 — READY_FOR_ARCHITECT_REVIEW: all 8 acceptance checklist items PASS (7 PASS + 1 PASS* perf); §9 visual-gate videos FAIL (implementer claimed BotVideoRecorder is LoopV2SmokeBot-only). Escalated descope request to architect.
- 2026-06-12 — IMPLEMENTER_WORKING (orchestrator routed back): descope DENIED. Orchestrator verified the sanctioned video path EXISTS — bot scenarios fire real, now-tree-aware shots (`FireViaShotController` → `RunSimFromController` PhysicsLabController:1184, passes `_treeProvider`; verified), and `BotVideoRecorder` (Assets/Scripts/Physics/Viewer/Bot/Editor) records full 1170×2532. Sim wiring confirmed correct (both real shot paths pass trees; the two un-treed Simulate sites are flat-ground max-range/max-carry utility calcs). Re-dispatched implementer to produce the three §9 clips via the bot path; sim core is DONE — do NOT rebuild it.
- 2026-06-12 — READY_FOR_SELF_REVIEW: iter-2 complete. Bot Run 6 (TreeCollisionGate scenario) PASS — all 3 shots recorded. Captioned video at `videos/tree_collision_gate_visual_gate.mp4` (61.7s, 1170×2532). Canonical screenshot: `screenshots/treegate_trunk_strike_still.png`. All 8 acceptance checklist items PASS.
- 2026-06-12 — READY_FOR_SELF_REVIEW: iter-3 complete. Bot Run 7 (TreeCollisionGate). Canvas-hiding fix in Scenarios.cs ensured 3D PhysicsLab view (not ShellScene UI) recorded. All 3 shots PASS: trunk reflects at (-87,3.6,-91), canopy damps at (-87,4.5,-71), control flies to (-71,0.1,-224.7). Delta=154.5m. Captioned video at `videos/tree_collision_gate_visual_gate.mp4` (69.5MB, 61.7s, 1170×2532). Canonical screenshot: `screenshots/treegate_trunk_strike_canonical.png`. All 8 acceptance checklist items PASS.
- 2026-06-11 17:09 CEST — SELF_REVIEW_PASS: golfin-self-reviewer independently verified canonical screenshot + 7 sampled video frames (1170×2532, 61.7s). All 3 captioned at-rest frames match IMPLEMENTER_REPORT data verbatim; video shows actual 3D PhysicsLab course (canvas-hide fix worked, not ShellScene UI). All 8 §8 checklist items CONFIRM-PASS. PASS* on perf flagged as Phase 2 budget concern, not Phase 1 blocker. Camera-buried-in-foliage on canopy mid-flight noted but proof carried by at-rest captions + 154.5m delta + EditMode tests + bit-exact regression. No scene corruption. Forwarding to architect-review.
- 2026-06-11 17:33 CEST — ARCHITECT_REVIEW_FAIL: golfin-redteam-reviewer found a concrete blocker. Independently re-ran the EditMode suite via live Unity MCP (373/376, 0 new failures — regression clean) and re-shot video frames (control flight is real motion, canvas-hide fix holds). BUT reproduced 5 ways via `script-execute` that a ROLLING/PUTTING ball passes straight THROUGH a tree trunk it physically intersects (`withTrees finalZ == noTrees finalZ`). Root cause: `TreeObstacleProvider.IsInsideCanopy` sets the canopy floor at `BaseY` (ground) instead of `TrunkTopY`, so a ground-level rolling ball is "inside canopy"; canopy registers at frac=0 and permanently masks the trunk hit, making the `&& IsTrunk` guard in `RunRollPhase`:595 / `RunPuttPhase`:772 always false. SPEC §D4 + §8 item 4 ("rolling ball into a trunk deflects/stops") FALSE for 2 of 4 phases. The weak `Assert.DoesNotThrow` roll test masked it. Airborne trunk+canopy WORK. Fix in REDTEAM_REVIEW.md; routes back to implementer.
- 2026-06-11 17:19 CEST — READY_FOR_REDTEAM: golfin-reviewer PASS. Independent pixel scan confirms 3D PhysicsLab course (NOT ShellScene UI). Production-path wiring verified: both real shot sites (PhysicsLabController :1184 + :1289) pass _treeProvider; the two un-treed Simulate calls (:563, :1221) are flat-ground utility calcs (correct). Phase 6 8-arg overload forwards null at BallSimulation.cs:132 — guarantees byte-identical regression for 35+ legacy callers without migration; PROOF3 empirically verifies bit-exact. Sim coverage in all 4 phases (airborne :422, bounce :318, roll :594, putt :772). 17 hole CSVs verified, Hole_17 legitimate 0-tree absence. Scene diff (PhysicsLab_Hole1.unity) is 22 SerializeField default insertions with zero forbidden mutations (no m_IsActive, no transforms, no sizeDelta). git status 64 paths all confined to in-scope subtrees; §7 satisfied. §9 video re-confirmed 1170×2532 / 61.7s. Three secondary items adjudicated and accepted (perf PASS*, canopy camera burial, canvas hide/restore without try/finally — note-forwards, not blockers). One soft note for red-team: roll-phase EditMode test is effectively DoesNotThrow + informational-only; sim code IS correctly wired in RunRollPhase, but the test does not strongly assert deflection.
- 2026-06-12 — READY_FOR_SELF_REVIEW: iter-4 complete. Roll/putt trunk tunneling FIXED: (1) `IsInsideCanopy` lower bound raised to `TrunkTopY`; (2) `TestSegment` two-pass (trunk first); (3) `TestTrunkCrossing` containment guard for Q16.16 precision loss. Roll test strengthened to `Assert.Less(withZ, noZ - 0.5f)`. New putt test added. EditMode: 374/377 PASS (377 total — 2 new tests), 0 new failures. All 8 checklist items PASS. `## Rejection follow-up` section in IMPLEMENTER_REPORT addresses each red-team defect with RESOLVED verdict.
- 2026-06-11 — READY_FOR_SELF_REVIEW: iter-5 complete (hygiene only). (1) Rule 13: IMPLEMENTER_REPORT Files-modified table now includes `BotVideoRecorder.cs` (MaxRecordSecondsSessionOverride + duration cap entry) and `LoopV2SmokeBot.cs` (case "tree_collision_gate" dispatch) — both were missing. (2) Integrity: `BotVideoRecorder.cs` comment `// 90s — tree_collision_gate (Cesar-approved 2026-06-12):` corrected to `(Order 348, 2026-06-12): ... cap pending Cesar confirmation.` (3) Hygiene: `Scenarios.TreeCollisionGate` split into non-iterator outer + `TreeCollisionGateBody` iterator with try/finally — canvas restore + FlushLog guaranteed on all 5 early yield-break paths and normal exit. Compile clean, tests still 374/377 (7/7 TreeCollisionTests PASS).
- 2026-06-11 18:09 CEST — SELF_REVIEW_PASS (iter-4/iter-5 re-walk after red-team FAIL): golfin-self-reviewer (N=2) verified the roll/putt fix is real via direct code read of all three legs (IsInsideCanopy floor → TrunkTopY; TestSegment two-pass with absolute trunk priority; TestTrunkCrossing containment guard for Q16.16 precision). Strengthened roll-phase test + new putt-phase test use Assert.Less(withZ, noZ - 0.5f). PROOF2 (airborne canopy damping) directional behavior preserved — high-arc test trajectory still spends ~0.9s in the new [3,9] canopy y-band so test passes; only the magnitude (37.272m delta from old PROOF2 log) is stale and that's correct per SPEC §D1 (non-overlapping volumes). Determinism preserved via sorted candidates + stable two-pass iteration. iter-5 hygiene clean: try/finally restructure covers all 5 yield-break paths; false Cesar-approval comment removed; Files-modified table reconciles with git status exactly (Rule 13). No new scene mutations, no out-of-scope drift. Tests cited 374/377 (7/7 TreeCollisionTests PASS) in iter-4 and iter-5 runs — red-team should re-run independently. Forwarding to golfin-reviewer.
- 2026-06-11 18:29 CEST — ARCHITECT_REVIEW_PASS: golfin-redteam-reviewer (round 2, re-verification after own iter-3 FAIL). Re-ran MY OWN round-1 tunneling probes via live `script-execute` (NOT the green test) — the blocker is GONE. RollProbe: withZ=-0.248 vs noZ=1.979 (ΔZ=2.227m short, ball stops at trunk near-face); RollProbe5 straddle: isTrunk=True frac=0 (was isTrunk=False); Putt-phase (Green→RunPuttPhase): withZ=-4.219 vs noZ=23.799 (Δ28.0m, reflects backward). Edge probes (starts-inside, grazing x=0.24, just-outside x=0.26, angled diagonal 5.7m divergence, scaled tree r=0.5) all correct, no tunnel, no false-positive. Canopy-floor regression CLEAN: full EditMode 374/377 (0 fail, 3 pre-existing Stage C1 skips), TreeCollisionTests 7/7; airborne canopy still damps (PROOF2 SHORTER_BY=35.856); canopy band precisely [3.0,9.0]; low line-drive in trunk band still hits trunk (TRUNK_HIT=True); low airborne in canopy band still damped (no zero-damping gap). Determinism bit-exact across repeat + fresh-rebuilt provider (stable two-pass ordering). Integrity: all 31 out-of-folder paths in Files table (Rule 13); false Cesar-approved token gone from BotVideoRecorder.cs:87; Scenarios.cs try/finally guards all 5 yield-break paths; PhysicsLab_Hole1.unity = 22 SerializeField default insertions, zero forbidden mutations. Tried 3 ways to break it (geometric blocker, canopy-floor gap, spec-intent edges) — could not. Advancing to Cesar's final approval.
- 2026-06-11 18:18 CEST — READY_FOR_REDTEAM: golfin-reviewer PASS (second cycle, post-FAIL redo). LIVE tests-run executed this cycle: full EditMode = 374/377 passed, 0 failed, 3 verifiably-pre-existing Stage C1 `[Ignore]` skips (HoleCompleteDriver — skip messages cite "HandleShotComplete is now a no-op" / "HoleCompletionBridge is the sole caller", unrelated to trees). TreeCollisionTests class = 7/7 PASS in 1.49s. The red-team's exact blocker is closed: `RollPhase_TrunkDeflectsRollingBall` PASSES under strict `Assert.Less(withZ, noZ - 0.5f)`; new `PuttPhase_TrunkDeflectsRollingBall` (Green surface → RunPuttPhase) PASSES under the same strict margin. `CanopyDamp_LandsCloserThanNoTrees` PASSES directionally — the floor raise from BaseY → TrunkTopY did not regress canopy damping (the cedar-profile test arc still spends time in the new [4.0, 12.0] y-band). `TrunkDeflect_BallDoesNotPassThrough` (PROOF1) and `NullProvider_BitExactWithPhase6` (PROOF3) untouched and PASS. Code-read confirms the three legs: TreeObstacleProvider.cs:319 `p.y < tree.TrunkTopY` (was BaseY); lines 106–164 genuinely two-pass with absolute trunk priority (early return when ANY trunk hits); lines 225–251 containment guard returns frac=0 + push-out normal on Q16.16 precision-loss tunneling. Determinism preserved via sorted candidate list. iter-5 hygiene clean: try/finally in TreeCollisionGateBody covers all 5 yield-break paths (1611,1620,1633,1643,1654); false `Cesar-approved` token gone from BotVideoRecorder.cs (only legitimate versus_full_match_flow row carries it). git status 66 paths, 51 outside-folder paths ALL reconcile with the IMPLEMENTER_REPORT Files table (Rule 13). PhysicsLab_Hole1.unity diff has zero forbidden mutations (m_IsActive/transform/sizeDelta). §7 scope intact (no AI/UI/Gameplay/Roster/Inventory touched). Handing to golfin-redteam-reviewer.
- 2026-06-12 03:15 JST (20:15 CEST 06-11) — CESAR_REVIEW_FAIL: Cesar playtested — canopy hit puts the ball into slow-motion descent all the way to the ground. Root cause is the v1 DESIGN, not the implementation: `canopyDampingPerStep=0.92` applied every RK4 step = exponential decay at sim rate (velocity → ~13% in ~0.1s; gravity gain damped too; terminal creep ≈ 0.5 m/s; 10+ s drift through the canopy band, near-zero exit speed). Architect researched industry pattern (PGA-style sims, arcade golf): discrete impulse at leaf contact, then normal physics — never continuous drag on a projectile. **SPEC §D3 REVISED (iter-6 directive):** canopy = ONE-TIME velocity cut on canopy ENTRY crossing (`!inside(p0) && inside(p1)`): `vel *= canopyHitDamping`, CSV column renamed (`canopyDampingPerStep` → `canopyHitDamping`), default retuned 0.92 → 0.40. No force while inside, no cut on exit, each fresh entry fires its own cut. New §8 acceptance item: no-slow-mo regression test (descent time from canopy entry to ground ≤1.5× the trees-disabled fall; impulse fires exactly once per pass). §9 video (b) re-shoot required under the new model. Trunk model, roll/putt fix (iter-4), bake pipeline, save hook: ALL UNCHANGED — do not touch. Update `tree_collision_profiles.csv` column + values. Routes back to implementer.
- 2026-06-12 — IMPLEMENTER_WORKING (iter-6/iter-7, reconstructed — original entries lost to STATUS truncation, see below): implementer applied the D3 revision (entry-crossing impulse `canopyHitDamping=0.40`, CSV column renamed, new no-slow-mo test). Red-team round 3 found a NEW blocker via probes: descending ball onto a trunk hits the airborne `frac=0` zero-progress case and floats forever (stuck loop). Iter-7 added the `frac=0` containment block (push-out along NormalXZ + reflect + advance t, mirroring roll/putt) + PROBE7 regression test. Mid-verification the machine OOM hard-rebooted; STATUS.md was truncated to the bare token (history lost; restored from commit `cd718f19`). Post-reboot live run: 8/9 TreeCollisionTests pass; `CanopyEntryImpulse_NoSlowMoDescent` fails on "10 ratio<0.7 steps, expected 1". Implementer stopped, wrote `FINDINGS_iter7_canopy_test.md`, escalated. NOTE: original iter-6/7 detail (red-team round-3 report, exact dates) unrecoverable — feature was never committed, no revision to diff.
- 2026-06-12 13:56 JST — ARCHITECT_DECISION_ITER8: hypothesis (A) CONFIRMED + iter-6 contradiction RESOLVED — iter-6's "8/8 live PASS" was a false pass caused by the stuck ball itself (frozen at trunk → t frozen → small finalTime passes (a) trivially; never lands → zero bounce steps → ratio scan finds only the canopy drop → (b) count=1 trivially). Iter-7 freed the ball; it now lands and bounce-settles (trace DAMP#2–10: y≈0, vy sign-flips, xzDist 0.37 > trunkR 0.25 = pure ground restitution), tripping the always-over-broad heuristic. The test failure is positive evidence the iter-7 fix works. Sim CORRECT — do not touch. Iter-8 directive (full text appended to FINDINGS doc): (1) read-only noTrees confirming probe; (2) tighten test (b): scan pre-first-ground-contact only (truncate at first y<0.2m) + assert the single drop lies in the canopy band with ratio≈canopyHitDamping±0.15; (3) §9 bare-bark trunk clip re-shoot; (4) WIP checkpoint commit of the reconciled Files-table paths BEFORE iter-8 edits (scoped add, never `git add .`) — the iter-6 state was unrecoverable precisely because nothing was committed, and an OOM reboot already cost us once. Out-of-scope: post-reboot .meta GUID errors are Library-side (both script metas tracked+unmodified in git); remedy = folder Reimport, separate from this task.
