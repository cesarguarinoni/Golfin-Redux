# STATUS — reward_points_backend

- **2026-08-12 (later 10) — ✅ SLICE 1 ACCEPTED (manual check passed).** Cratilo recreated as `cesar.guarinoni@gmail.com` (original was deleted during the auth epic — auth.users audit confirmed only 4 accounts remained; signup trigger created the profiles row correctly). Admin-granted 123 RP via `earn_pts_v2` ('manual_admin_grant' — first live use of the admin-grant workflow that replaces the welcome grant). Editor check: `[PointsBackend] ✅ Server balance for cesar.guarinoni@gmail.com: RP=123 (activity=123, gift=0, avatar L1/123xp)` — full chain verified (Editor → AuthService token → ApiClient → playlife-api → Supabase). In-game RP counter still shows local balance BY DESIGN (Slice 1 = zero behavior change; re-pointing is Slice 2). Note: Google/Apple sign-in cannot complete in the Editor (deep-link receiver exists only in builds) — email/password is the Editor test path; worth remembering for all future Editor-side auth testing. Remaining before Slice 2 runs: flag back OFF, commit both repos, paste the Slice 2 kickoff (archived below/TellCode).

- **2026-08-12 (later 9) — ✅ SLICE 1 LANDED (Code) → SLICE 2 KICKOFF ISSUED.** Slice 1 report: Golfin.Net + Golfin.Economy in, 46 new tests (named-verified), EditMode 1159 passed / 0 failed, zero behavior change proven (no references to new types anywhere existing; flag-OFF paths make 0 HTTP/disk/GameObject). Contract corrections recorded from the live service: `/health` is root-mounted (not under /api/v1); 403 = missing auth header (no refresh), 401 = refresh-and-replay; errors are `{detail}` not `{data}`; earn payload carries no gift_pts (cached gift bucket carried forward). Auth refresh reused from AuthService.cs:126 via ~30-line AuthServiceTokenProvider. **Cesar's manual items:** (1) Editor acceptance — play mode signed in → GOLFIN > Points Backend > Enabled → Log Server Balance Now → expect balance log, then toggle OFF; (2) commit BOTH repos (GolfinRedux Slice 1 + playlife Phase A files — nothing is committed anywhere yet). Slice 2 kickoff (rebalance + re-point + cutover, catalog-mirror SQL write-only) archived in TellCode.md.

- **2026-08-12 (later 8) — ✅ PHASE A SHIPPED TO PROD, end to end.** Deploy completed: flyctl v0.4.82 installed, Cesar authenticated (wonderwall account — no GitHub account involved; one-time Fly "high-risk" unlock completed by Cesar), `fly deploy` green (both machines updated, DNS verified). Live verification: `GET /health` → `{status: ok}`; `POST /points/spend`, `POST /points/earn-game`, `GET /points/balance` all return **403 (auth-gated), not 404** — new code is serving. Phase A gate and rebalance gate are BOTH clear → **Slice 2 kickoff unblocks as soon as Slice 1 (Unity infra) lands.**

- **2026-08-12 (later 7) — ✅ GIFT BUG FIXED IN PROD** (Cesar-approved "fix everything now"). New migration `backend/migrations/2026_08_12_gift_pts_total_points_fix.sql` (written to the playlife repo + applied via Supabase SQL editor): `add_gift_pts_to_receiver()` now credits `total_points` alongside `gift_pts`; EXECUTE revoked from public/anon/authenticated (trigger firing unaffected — checked at creation, not fire time); one-time reconciliation UPDATE ran. Verified: invariant `total_points = activity_pts + gift_pts` holds on **0** profiles broken; the 2 affected accounts now read 425+50=**475** and 580+100=**680**; trigger no longer client-executable. **Deploy in progress:** flyctl v0.4.82 installed to `~/.fly/bin`; `fly auth login` requires an interactive terminal — waiting on Cesar to complete browser auth; then `fly deploy` from `backend/` (app playlife-api, nrt).

- **2026-08-12 (later 6) — ✅ MIGRATION APPLIED TO PROD** (Architect via Cesar's browser session, Supabase SQL editor, project wmszyghwwkaptgqdunel). Verify-first passed (`earn_activity_pts` + `apply_score_submit` present, service_role-only). `2026_08_12_points_spend_idempotency.sql` ran clean ("Success. No rows returned"). Post-apply verified: `earn_pts_v2` + `spend_pts` service_role-only ✓, `points_transactions.idempotency_key` (uuid) + partial unique index ✓, catalog = 3 rows (`hole_complete` 10/day-cap 500, `versus_win` 30/day-cap 500, `tournament_prize` client-amount/day-cap 5000 — placeholders pending Slice-2 mirror). `golfin_welcome` + `legacy_balance_migration` seed rows DELETED per decision #6. **Gift-bug detection ran: 2 profiles affected, each 100 pts missing from total_points** (425+50 vs 375; 580+100 vs 580) — fix + reconciliation awaiting Cesar's go (also found: `add_gift_pts_to_receiver` is EXECUTE-able by `authenticated` — revoke recommended in the same pass). **NOT yet deployed**: flyctl not installed on the Mac; endpoints in points.py are inert until `fly deploy` (safe — RPCs now exist, correct order preserved).

- **2026-08-12 (later 5) — REBALANCE APPROVED (with amendment) + design simplifications.** Cesar approved `RP_REBALANCE.md` (÷10 global, ceil(level/2) level-ups, stamina rounding rule, §3 caps) with the **welcome grant REMOVED** — it was testing-only; new accounts start at 0 RP, test balances are admin-set (dashboard points panel later, Supabase table editor now). Consequences: no `golfin_welcome`/`legacy_balance_migration` actions, no client migration logic (SPEC §5 rewritten), Slice 2 additionally deletes `DEFAULT_STARTING_POINTS` from `RewardPointsManager.Awake`. Also of record: **Cesar runs the GPS app now** — all former Ken sign-offs are Cesar's calls. Slice 2 kickoff pending Phase A landing.

- **2026-08-12 (later 4) — RP_REBALANCE.md DRAFT delivered, awaiting Cesar's approval.** Full ÷10 conversion (round half-up, min 1) across HoleDatabase.csv, modes.csv (incl. entry fees), tournaments.csv entryFeeRP, tournament_prizes.csv, LevelUpCosts.csv (proposal: ceil(level/2)), gacha_banners.csv, shop_catalog.csv, stamina_shop_items.csv, debug panel, welcome grant 50,000→5,000 (recommended bucket: gift_pts). Final `game_point_actions` seed values included (§3). Four open flags at §5. File: `RP_REBALANCE.md` in this folder. On approval → Slice 2 kickoff. NOTE: the Filesystem MCP broke mid-session (schema error) — this file and RP_REBALANCE.md were written via the Cowork folder bridge instead.
- **2026-08-12 (later 3) — DESIGN OVERRIDE by Cesar: ONE shared RP value.** No `golfin_rp` currency — RP == PLAYLIFE `total_points`, avatar-XP coupling kept, and the game-economy **rebalance to GPS scale is folded into this task** (Slice 2 = rebalance + cutover together, gated on Cesar-approved `RP_REBALANCE.md`; Architect drafts it next). SPEC §3/§4/§5/§7 rewritten; Phase A kickoff REVISED in TellCode.md (migration renamed `2026_08_12_points_spend_idempotency.sql`; `earn_pts_v2` + `spend_pts`). Any `golfin_rp` kickoff copy is dead.
- **2026-08-12 (later 2) — Phase A UNBLOCKED + kickoff issued.** Repo located: `/Users/cesar/Documents/playlife` (fresh copy on the Mac). Real `points.py` + `2026_06_29_points_atomic.sql` read → design change of record: RP = own currency `golfin_rp` in the shared ledger (earn_activity_pts couples earns to avatar XP at 5–50-pt scale — unusable for 50k-scale RP). SPEC §2/§3 rewritten against real code. Phase A kickoff archived in TellCode.md. [Superseded by later 3.]
- **2026-08-12 — SPEC_READY (Slice 1 kickoff issued).** Decisions locked: points before dashboard; online spends + queued earns; unify on the PLAYLIFE ledger (GPS_UNITY_PORT_SPEC §2 answered). Slice 1 = Unity infra behind a default-OFF flag (Golfin.Net ApiClient + PointsService + pending-ops queue), zero behavior change.
- Next state: `IN_PROGRESS` when Claude Code starts Slice 1 and/or Phase A (independent repos, can run in either order or parallel). Slice 2 kickoff comes after rebalance approval + Phase A.
