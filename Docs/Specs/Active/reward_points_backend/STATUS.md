# STATUS — reward_points_backend

- **2026-08-12 (EOD) — ✅ FOLLOW-UPS LANDED + committed. Task is one device pass from Completed.** All three: (1) BotSessionOverride (own Golfin.DevHarness assembly; fake session + forced-flag-OFF as one indivisible act; namespace auto-detection covers Physics/ bots zero-edit; InitializeOnLoad disarm on both play-mode edges — fixes the domain-reload-disabled leak AND a latent session.Clear() that deleted the real persisted session after every bot run) — TournamentLoopCaptureHarness reached SEQUENCE COMPLETE twice from boot; (2) shop spends via PointsSpendGate (server-first, grant-on-approval); (3) hard sign-in gate via AuthGate on the ShowScreen seam, deny-by-default — and it killed **DevBypassCatcher_TEMP, an invisible tap-catcher that shipped in player builds and sent any tap to Home with no auth**. EditMode 1172/0 full sweep, re-run after the disarm fixes. **REMAINING before moving this spec to Completed: 3 device checks** (signed-out launch tapping around the art; flag-ON purchase stays debited after refresh + airplane-mode refusal; double-tap BUY) — see IMPLEMENTER_REPORT. Then `admin_dashboard` comes off hold. Foreign dirt still uncommitted by design: ShellScene.unity, Scenarios.cs, SignUpScreenController.cs, UI_HIERARCHY.md (another session's auth-flow work; SplashScreenController.cs is shared — committed carrying their additive changes).

- **2026-08-12 (later 13) — ✅ `points_cutover_followups` LANDED (Code): bot bypass + shop server spend + hard sign-in gate.** All three of Cesar's follow-up decisions implemented; EditMode **1172 passed / 0 failed / 3 pre-existing skips of 1175** (full unfiltered run, identical to the Slice 2 baseline). **(1) Bot auth bypass** — new `Golfin.Dev.BotSessionOverride` (own `Golfin.DevHarness` asmdef, whole-file `#if UNITY_EDITOR || GOLFIN_BOT_HARNESS`) installs a fake local session AND forces `PointsBackendEnabled` OFF as one indivisible act, so a bot can never aim a real ledger call at prod under a fake token. Armed explicitly by `TournamentLoopCaptureHarness`, and auto-armed by the presence of any live `Golfin.Physics.Viewer.Bot` host — which is how every legacy bot is covered with **ZERO edits under `Assets/Scripts/Physics/`** (standing ban). `BotDriver` is untouched: the override changes what `StartButton` does, not what the bot clicks. **Acceptance met: `TournamentLoopCaptureHarness` reaches `=== SEQUENCE COMPLETE ===` from boot** (two full dry runs, 2 holes played to cup each). **(2) Shop server spend** — both `ShopTransaction` entry points now debit through `PointsSpendGate` (server first, grant only on approval) and report by callback; new `SpendDenied` verdict, per-controller in-flight latch, and the callers stay silent on refusal because the gate owns the toast. New free-form spend reasons `stamina_boost` / `shop_purchase` — **no backend change** (only earn actions are catalog-validated). The self-refunding flag-ON purchase is closed. **(3) Hard sign-in gate** — deleted `DevBypassCatcher_TEMP` (an invisible full-screen tap-catcher that shipped in player builds and sent any stray tap to Home with no auth) and added `AuthGate` on the same `ScreenManager.ShowScreen` seam `DemoGate` uses: deny-by-default, so a screen added later is gated rather than silently reachable; three ways through (real session / `GOLFIN_DEMO` build / bot override); blocked navigation redirects to Login. Verified in play mode signed out: real `StartButton.onClick` settles on **Login** (LoginScreen active, HomeScreen not), `ShowScreen(Home)` and `ShowScreen(ModeSelection)` both redirect to **Login**, `DevBypassCatcher_TEMP` gone. **Bug found and fixed mid-task:** the first harness run proved `SessionForcedOff` LEAKED — this project runs with domain reload disabled, so the static survived play-mode exit and left `PointsBackendFlag.Enabled=false` in the Editor against a compiled default of true (and would have carried a fake "Bot" session into the next normal Play). Fixed with an `InitializeOnLoad` disarm on every play-mode edge; also fixed a latent `session.Clear()` that would have deleted the PERSISTED (real) session from PlayerPrefs at the end of a bot run. Verified after the fix: `SessionForcedOff=False`, `Enabled=True`, no stored pref written, no persisted session, ShellScene never saved. **Needs Cesar (device):** signed-out launch cannot pass Login (tap around the art, not just buttons); flag-ON shop purchase stays debited after a refresh + airplane-mode refusal grants nothing; double-tap BUY is a no-op. **Note:** the working tree was already dirty on arrival with another session's auth-flow work (`SplashScreenController.cs` is shared — my edits are additive, see IMPLEMENTER_REPORT Part 3 § Pre-existing drift).

- **2026-08-12 (later 12) — Follow-up decisions locked (Cesar) + `points_cutover_followups` kickoff issued.** (1) Bot auth = dev-only BotSessionOverride (fake signed-in session + PointsBackendEnabled forced OFF; editor/define-guarded, unshippable) — restores every boot-from-Splash harness; (2) ShopTransaction routes through SpendAsync NOW (closes the self-refund hole); (3) HARD sign-in gate, no guest mode. Kickoff archived in TellCode.md. Catalog SQL applied+verified in prod; hole_replay label deployed; both repos committed+pushed (playlife 37f27d9, GolfinRedux f30787e71).

- **2026-08-12 (later 11) — ✅ SLICE 2 LANDED (Code) + CATALOG SQL APPLIED + LABEL DEPLOYED.** EditMode 1172/0 (16-assembly sweep; ⚠️ lesson recorded: filtered runs report FailedTests only for the filter — sweep per assembly). Tournament sign-up verified on real widgets both flag directions (fee exactly 10; no-session → 403 → no debit/entry/navigation). Architect follow-through same day: `2026_08_12_game_point_actions_rebalance.sql` APPLIED to prod + verified (4 rows exact: hole_complete -/20/400, hole_replay -/5/100, versus_win 20/20/200, tournament_prize -/2000/-; retired rows gone — replay earns no longer dropped); `points.py` GAME_ACTION_LABELS +hole_replay, deployed to Fly. RP_REBALANCE §2 prose corrected: ceil(level/2) sums to **14,520**, not 14,460 (Code caught the 60-pt drift; formula ships). **OPEN — Cesar decisions:** (1) ShopTransaction still spends local-only (flag-ON purchase self-refunds on next refresh — fix before shop is player-visible); (2) not-signed-in players can reach mode select but can spend nothing (guest policy needed); (3) bot/harness auth — BotDriver stalls at the mandatory Login screen; solution needed that avoids real credentials. TournamentLoopCaptureHarness broken until (3).

- **2026-08-12 (later 11) — ✅ SLICE 2 CODE COMPLETE (rebalance + re-point + flag flip). AWAITING CESAR'S MANUAL CUTOVER.** Economy rebalanced verbatim from `RP_REBALANCE.md` across 8 CSVs (101 value edits + all 240 `LevelUpCosts` rows) plus two code-side CSV mirrors that would otherwise reinstate the old economy on a load failure (`ModesDatabaseCSV.AddFallbackModes`, `VersusResultHandler._fallbackReward`). Earns: `EarnPoints(int)` is GONE — the only signature is `EarnPoints(int, string action)`, so the compiler forced all four earn sites to declare `hole_complete` / `hole_replay` / `versus_win` / `tournament_prize`; local balance, leaderboard accumulators and RP SFX unchanged, queue reconciles. Spends: new `PointsSpendGate` puts the server debit in front of the action in all four flows (character + club level-up, tournament sign-up via the new `IRewardPointsService.TrySpendAsync`, mode entry fee); `LocalTournamentBackend.Register` untouched (modal pays first, passes fee 0, and short-circuits on `GetMyEntry` to keep Register's no-re-charge idempotence); process-wide in-flight latch prevents double-tap double-charge; offline → "Connection required", 200-insufficient → "Not enough Reward Points". Client seed DELETED from `RewardPointsManager.Awake` (decision #6); `SetPoints`/`ResetToDefault`/debug panel/`RosterDebugTools` grant are now flag-OFF-only. **`PointsBackendEnabled` default flipped OFF → ON**, last, after green. EditMode **1172 passed / 0 failed / 3 pre-existing skips of 1175** (swept per-assembly across all 16 assemblies — a filtered run reported 0 failures while a real one existed, see the report's warning). Catalog-mirror SQL WRITTEN, NOT APPLIED: `playlife/backend/migrations/2026_08_12_game_point_actions_rebalance.sql`. **Three things need Cesar** (full list in `IMPLEMENTER_REPORT.md` Part 3 §7 + the manual steps): (1) apply the catalog SQL — until then `hole_replay` is an unknown action server-side and those earns are silently dropped; (2) hand-set the 5 test balances in Supabase — new accounts now start at 0; (3) on-device smoke with the flag ON. **Two open product questions surfaced:** `ShopTransaction` (general + stamina shop) still spends local-only and will desync under the flag, and a not-signed-in player can no longer spend anything at all.

- **2026-08-12 (later 10) — ✅ SLICE 1 ACCEPTED (manual check passed).** Cratilo recreated as `cesar.guarinoni@gmail.com` (original was deleted during the auth epic — auth.users audit confirmed only 4 accounts remained; signup trigger created the profiles row correctly). Admin-granted 123 RP via `earn_pts_v2` ('manual_admin_grant' — first live use of the admin-grant workflow that replaces the welcome grant). Editor check: `[PointsBackend] ✅ Server balance for cesar.guarinoni@gmail.com: RP=123 (activity=123, gift=0, avatar L1/123xp)` — full chain verified (Editor → AuthService token → ApiClient → playlife-api → Supabase). In-game RP counter still shows local balance BY DESIGN (Slice 1 = zero behavior change; re-pointing is Slice 2). Note: Google/Apple sign-in cannot complete in the Editor (deep-link receiver exists only in builds) — email/password is the Editor test path; worth remembering for all future Editor-side auth testing. Remaining before Slice 2 runs: flag back OFF, commit both repos, paste the Slice 2 kickoff (archived below/TellCode).

- **2026-08-12 (later 9) — ✅ SLICE 1 LANDED (Code) → SLICE 2 KICKOFF ISSUED.** Slice 1 report: Golfin.Net + Golfin.Economy in, 46 new tests (named-verified), EditMode 1159 passed / 0 failed, zero behavior change proven (no references to new types anywhere existing; flag-OFF paths make 0 HTTP/disk/GameObject). Contract corrections recorded from the live service: `/health` is root-mounted (not under /api/v1); 403 = missing auth header (no refresh), 401 = refresh-and-replay; errors are `{detail}` not `{data}`; earn payload carries no gift_pts (cached gift bucket carried forward). Auth refresh reused from AuthService.cs:126 via ~30-line AuthServiceTokenProvider. **Cesar's manual items:** (1) Editor acceptance — play mode signed in → GOLFIN > Points Backend > Enabled → Log Server Balance Now → expect balance log, then toggle OFF; (2) commit BOTH repos (GolfinRedux Slice 1 + playlife Phase A files — nothing is committed anywhere yet). Slice 2 kickoff (rebalance + re-point + cutover, catalog-mirror SQL write-only) archived in TellCode.md.

- **2026-08-12 (later 8) — ✅ PHASE A SHIPPED TO PROD, end to end.** Deploy completed: flyctl v0.4.82 installed, Cesar authenticated (wonderwall account — no GitHub account involved; one-time Fly "high-risk" unlock completed by Cesar), `fly deploy` green (both machines updated, DNS verified). Live verification: `GET /health` → `{status: ok}`; `POST /points/spend`, `POST /points/earn-game`, `GET /points/balance` all return **403 (auth-gated), not 404** — new code is serving. Phase A gate and rebalance gate are BOTH clear → **Slice 2 kickoff unblocks as soon as Slice 1 (Unity infra) lands.**

- **2026-08-12 (later 7) — ✅ GIFT BUG FIXED IN PROD** (Cesar-approved "fix everything now"). New migration `backend/migrations/2026_08_12_gift_pts_total_points_fix.sql` (written to the playlife repo + applied via Supabase SQL editor): `add_gift_pts_to_receiver()` now credits `total_points` alongside `gift_pts`; EXECUTE revoked from public/anon/authenticated (trigger firing unaffected — checked at creation, not fire time); one-time reconciliation UPDATE ran. Verified: invariant `total_points = activity_pts + gift_pts` holds on **0** profiles broken; the 2 affected accounts now read 425+50=**475** and 580+100=**680**; trigger no longer client-executable. **Deploy in progress:** flyctl v0.4.82 installed to `~/.fly/bin`; `fly auth login` requires an interactive terminal — waiting on Cesar to complete browser auth; then `fly deploy` from `backend/` (app playlife-api, nrt).

- **2026-08-12 (later 6) — ✅ MIGRATION APPLIED TO PROD** (Architect via Cesar's browser session, Supabase SQL editor, project wmszyghwwkaptgqdunel). Verify-first passed (`earn_activity_pts` + `apply_score_submit` present, service_role-only). `2026_08_12_points_spend_idempotency.sql` ran clean ("Success. No rows returned"). Post-apply verified: `earn_pts_v2` + `spend_pts` service_role-only ✓, `points_transactions.idempotency_key` (uuid) + partial unique index ✓, catalog = 3 rows (`hole_complete` 10/day-cap 500, `versus_win` 30/day-cap 500, `tournament_prize` client-amount/day-cap 5000 — placeholders pending Slice-2 mirror). `golfin_welcome` + `legacy_balance_migration` seed rows DELETED per decision #6. **Gift-bug detection ran: 2 profiles affected, each 100 pts missing from total_points** (425+50 vs 375; 580+100 vs 580) — fix + reconciliation awaiting Cesar's go (also found: `add_gift_pts_to_receiver` is EXECUTE-able by `authenticated` — revoke recommended in the same pass). **NOT yet deployed**: flyctl not installed on the Mac; endpoints in points.py are inert until `fly deploy` (safe — RPCs now exist, correct order preserved).

- **2026-08-12 (later 5) — REBALANCE APPROVED (with amendment) + design simplifications.** Cesar approved `RP_REBALANCE.md` (÷10 global, ceil(level/2) level-ups, stamina rounding rule, §3 caps) with the **welcome grant REMOVED** — it was testing-only; new accounts start at 0 RP, test balances are admin-set (dashboard points panel later, Supabase table editor now). Consequences: no `golfin_welcome`/`legacy_balance_migration` actions, no client migration logic (SPEC §5 rewritten), Slice 2 additionally deletes `DEFAULT_STARTING_POINTS` from `RewardPointsManager.Awake`. Also of record: **Cesar runs the GPS app now** — all former Ken sign-offs are Cesar's calls. Slice 2 kickoff pending Phase A landing.

- **2026-08-12 (later 4) — RP_REBALANCE.md DRAFT delivered, awaiting Cesar's approval.** Full ÷10 conversion (round half-up, min 1) across HoleDatabase.csv, modes.csv (incl. entry fees), tournaments.csv entryFeeRP, tournament_prizes.csv, LevelUpCosts.csv (proposal: ceil(level/2)), gacha_banners.csv, shop_catalog.csv, stamina_shop_items.csv, debug panel, welcome grant 50,000→5,000 (recommended bucket: gift_pts). Final `game_point_actions` seed values included (§3). Four open flags at §5. File: `RP_REBALANCE.md` in this folder. On approval → Slice 2 kickoff. NOTE: the Filesystem MCP broke mid-session (schema error) — this file and RP_REBALANCE.md were written via the Cowork folder bridge instead.
- **2026-08-12 (later 3) — DESIGN OVERRIDE by Cesar: ONE shared RP value.** No `golfin_rp` currency — RP == PLAYLIFE `total_points`, avatar-XP coupling kept, and the game-economy **rebalance to GPS scale is folded into this task** (Slice 2 = rebalance + cutover together, gated on Cesar-approved `RP_REBALANCE.md`; Architect drafts it next). SPEC §3/§4/§5/§7 rewritten; Phase A kickoff REVISED in TellCode.md (migration renamed `2026_08_12_points_spend_idempotency.sql`; `earn_pts_v2` + `spend_pts`). Any `golfin_rp` kickoff copy is dead.
- **2026-08-12 (later 2) — Phase A UNBLOCKED + kickoff issued.** Repo located: `/Users/cesar/Documents/playlife` (fresh copy on the Mac). Real `points.py` + `2026_06_29_points_atomic.sql` read → design change of record: RP = own currency `golfin_rp` in the shared ledger (earn_activity_pts couples earns to avatar XP at 5–50-pt scale — unusable for 50k-scale RP). SPEC §2/§3 rewritten against real code. Phase A kickoff archived in TellCode.md. [Superseded by later 3.]
- **2026-08-12 — SPEC_READY (Slice 1 kickoff issued).** Decisions locked: points before dashboard; online spends + queued earns; unify on the PLAYLIFE ledger (GPS_UNITY_PORT_SPEC §2 answered). Slice 1 = Unity infra behind a default-OFF flag (Golfin.Net ApiClient + PointsService + pending-ops queue), zero behavior change.
- Next state: `IN_PROGRESS` when Claude Code starts Slice 1 and/or Phase A (independent repos, can run in either order or parallel). Slice 2 kickoff comes after rebalance approval + Phase A.
