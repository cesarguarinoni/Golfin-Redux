# reference/gates.txt — build_size_diet, the two gates
# Instrument: Docs/Scripts/measure_ios_data.sh (per-file raw + per-file deflate, plus the
# constant non-Data Payload remainder measured off the shipped .ipa). Reproduces the shipped
# Builds/ipa/Golfin.ipa to within 0.3%.

                              INSTALL          PAYLOAD-COMPRESSED     .ipa FILE
                       (Payload uncompressed)  (what a tester gets)   (reported, not gated)
  gate                        <= 1024 MiB          <= 350 MiB              --

  BEFORE  (.ipa 2632)           1844.7 MiB           557.1 MiB          678.3 MiB
  BEFORE  (local build 2643)    1839.5 MiB           555.4 MiB
  AFTER   (local build 2655)    1009.1 MiB           304.6 MiB
  AFTER   (.ipa 2658, SHIPPED)  1008.9 MiB           305.2 MiB          426.4 MiB
  delta (.ipa to .ipa)          -835.8 MiB           -251.9 MiB         -251.9 MiB
                                  (-45%)               (-45%)             (-37%)

  INSTALL:            PASS, 15.1 MiB inside the gate.
  PAYLOAD-COMPRESSED: PASS, 44.8 MiB inside the gate.

  Both gates are confirmed on the SHIPPED ARCHIVE, not only on the local model: build 2658 was
  archived and uploaded to TestFlight on 2026-09-04 and `unzip -lv` on it gives the AFTER row
  above. The model predicted 1009.1 / 304.6 and the real archive came back 1008.9 / 305.2 —
  0.2 and 0.6 MiB out, which is what makes the per-phase numbers in this file trustworthy.
  (2658 is the GPS variant: iOS-Full-GPS differs from iOS-Full by one scripting define and
  carries identical assets, so every size line is the game's.)

  The .ipa FILE is 678.3 -> 426.4 MiB. It is still not a gate, and 121.2 MiB of it is the
  `Symbols/` payload Apple keeps and strips before a tester downloads anything.

  Build 2654 landed at 1035.9 MiB — 11.9 MiB OVER — with the bridge budget at
  albedo 2048 / normal 1024. Rather than round that off or call it close, the budget was
  halved again to albedo 1024 / normal 512 and the A/B rig was asked whether that is
  visible. It is not: see ab_frame_diff_tight.txt and
  screenshots/bridge_cap_crop_2048_vs_1024.png. Build 2655 is that answer.

== Where the -803 MiB came from (Unity Build Report, before -> after) ==

  Assets/Packs/PBR Bridge     460.7 ->  17.3 MiB   -443.4   Phase 1 texture budget
  Assets/Resources/HoleData   387.4 ->  45.7 MiB   -341.7   Phase 2 GHM2 + gzip zones
  Assets/Resources/Clubs       55.4 ->  17.3 MiB    -38.1   Phase 2.7 cap at 512
  Assets/Resources/Art          5.0 ->   0.6 MiB     -4.5   Phase 3 uncompressed sweep
  Assets/Art/UI                41.7 ->  39.5 MiB     -2.3   Phase 3
  everything else                              ~     -0.9
  total listed user assets   1610.5 -> 780.7 MiB   -829.8

== What is left ==

  509.7 MiB  Assets/Golf/Courses      the 18 TerrainData, 28.3 MiB each. 63% of everything
                                      that remains. The only lever is Phase 5, and Phase 5 is
                                      a change to the resolution at which OUT OF BOUNDS is
                                      decided — see terrain_and_prototypes.txt. Not taken.
   45.7 MiB  Assets/Resources/HoleData    already 8-13x compressed, losslessly
   17.3 MiB  Assets/Packs/PBR Bridge      capped twice, 4096 -> 1024 albedo
   39.5 MiB  Assets/Art/UI                the screens the Figma work is built on
   22.8 MiB  Assets/Art/3D                the trees that actually ship
    8.7 MiB  NotoSansJP TTF               Phase 4 option (c) would take this to 2.56 MiB

  Both gates are met without any of the decisions still open with Cesar. Each of those
  would add margin on top:
    - Phase 0b LZ4HC          measured -63% on the PRE-diet tree; on this one it would take
                              install far below the gate. A build option, nothing visible.
    - Phase 4 option (c)      a further -6.15 MiB.
    - Phase 5                 a further ~-120 MiB, but see terrain_and_prototypes.txt: as
                              specified it changes the resolution at which OB is decided.

== The standalone (iOS-Standalone), built to prove Phase 2 did not break the stash ==

  INSTALL 226.5 MiB / PAYLOAD-COMPRESSED 78.8 MiB (reference/data_standalone_after.txt).
  StandaloneBuildPreprocessor moves the HoleData FOLDER by name, so the zones.json ->
  zones.bytes rename is invisible to it; verified after the build that the sentinel is gone
  and Hole_01 came back with GHM2 heightmap.bytes and zones.bytes intact.
