# STATUS — tournaments_server_side

- **2026-08-13 — SPEC_READY (Phase 1–2).** Filed from Cesar's "Add tournaments to the Admin". Investigation first: server `tournaments`/`tournament_entries` exist but are EMPTY and GPS-shaped (trust/membership/real-round columns); the game's tournaments are a shipped CSV with course/holeSet/botField/rank-band concepts the server table has no room for. Cesar's call: **move them server-side**, spec before building. Awaiting his approval of the §4 schema shape, then the Architect builds the Phase 2 panel; Phase 3 (Unity reads server) is Claude Code's, own kickoff.
- **2026-08-13 (later) — §5b added: tournament images.** Cesar asked what happens to in-game tournament art. Found `ResolveSprite` uses an Inspector-wired map plus a **positional** `_courseImages[csvIndex]` fallback — a dashboard-created tournament would show another course's photo, and reordering would reshuffle art. Fix is cheap: the art at `Assets/Art/Tournaments/CourseImages/` is ALREADY named by courseId (1:1 with all six), it just is not under `Resources/`. Phase 3 loads by convention and deletes the positional fallback; Phase 2 makes `course_id` a dropdown of shipped courses. Bundled art is sufficient permanently (a tournament can only run on a course that ships); `banner_url`/remote promo art deferred.
