ARCHITECT_REVIEW_PASS

# STATUS — loop_v2_c0_matchmaking_to_gameplay_transition

**Status:** ARCHITECT_REVIEW_PASS (architect-reviewer, 2026-05-19 JST)
**Type:** TELLCODE — Cesar-visual gate is heavier than typical TELLCODE (first end-to-end production playthrough)
**Parent:** `Docs/Specs/Active/loop_v2_scope/SPEC.md` (Stage C splits into C0 + C1)
**Root cause:** `Docs/Specs/Queued/ARCHITECT_NOTE_matchmaking_to_gameplay_transition.md` (Stage B finding)
**Notion:** Loop v2 Order 330 (sub-item of 300)

## History
- 2026-05-19 — Stage B surfaced the missing transition: no production code loads any gameplay scene.
- 2026-05-19 (architect) — SPEC.md written. Three architecture decisions locked: D1 LabScaffold as gameplay host scene, D2 additive load, D3 all 18 holes via existing LoadHole(n).
- 2026-05-19 (implementer) — All audit grep PASS. Build settings: LabScaffold + 18 Hole_NN_Geo added, ghost Hole_01.unity removed. GameplaySceneLoader created. LoadingScreenController generalized with LoadTarget enum. MatchmakingModalController.OpponentScanRoutine hands off to BeginGameplayLoad. PersistentUIManager.SetBottomNavVisible added as alias. PhysicsLabController.Start logs GameSession.CurrentHoleNumber; ScanForLoadedHoleSceneAtStartup now polls 5s for the seeded hole. 5 new EditMode tests pass; full suite 305/305. Compile clean. Scene wiring is manual, paste-for-Cesar steps in IMPLEMENTER_REPORT.md. Cesar visual gate is the canonical proof per SPEC.
- 2026-05-19 (self-reviewer) — FORWARD_TO_ARCHITECT. All mechanical DoD greps confirmed independently. Risk-area traces sound (5s poll timing, `_isLoading` ordering, reflection tests exercise real production paths). Open Question 1 (HoleSelection PLAY entry) resolved via grep — both Home and HoleSelection pass an explicit hole index, so holes 2-18 ARE reachable. Open Questions 2-4 appropriately surfaced for Cesar's visual gate. Scene-mutation audit clean. One procedural observation about the screenshot capture path bypassing CaptureCore's IsCreated() guard — surfaced as observation, not blocked given the SPEC explicitly de-emphasizes the screenshot's load-bearing role.
- 2026-05-19 (architect-reviewer) — APPROVE_FOR_CESAR. Cross-cutting checks all PASS: GameplaySceneLoader/PhysicsLabController split clean (both production and editor funnel through `OnHoleLoaded(sceneName)`); LoadingScreenController generalization doesn't leak C0-specific assumptions into legacy path; SetBottomNavVisible alias acceptable with minor smell (collapse follow-up); 5s polling is bounded with graceful timeout-fallback (replace with sceneLoaded event in Stage D / cleanup follow-up); Spec deviation §1 leaves no Stage C1/D sharp edge (UnloadGameplay symmetric teardown already exists); git diff confirms zero scene mutations (only EditorBuildSettings.asset touched per SPEC); full Spec coverage with five well-reasoned deviations. Capture-helper bypass surfaced but non-gating (no scene corruption resulted; queued capture_core fallback ticket is the proper home). See ARCHITECT_REVIEW.md.

## Notes for Cesar
- This is the first end-to-end production playthrough stage in project history. Visual gate is unavoidably Cesar's manual playthrough; the implementer cannot run it from EditMode.
- Before the visual gate, the manual scene-wiring steps in IMPLEMENTER_REPORT.md §"Scene wiring (paste-for-Cesar)" must be completed: add `GameplaySceneLoader` to ShellScene (Awake auto-wires if SerializeFields are left null), verify `PersistentUIManager.bottomNavPanel` is wired to the bottom-nav root.
- Possible visual concern Cesar should watch for: double-fade flicker between Matchmaking modal Hide() and ScreenManager.ShowScreen(Loading) fade-in (OQ3). If it surfaces, the C0-localized fix is `ShowScreen(ScreenId.Loading, instant: true)` in `GameplaySceneLoader.ApplyPreloadSetup`.
- Follow-up tickets surfaced by architect review (do not gate C0): (a) consolidate `LoadingScreenController.SetRealProgress` / `SetProgress` duplicate; (b) collapse `PersistentUIManager.SetBottomNavVisible` into `ShowBottomNav` in audit P1 sweep; (c) replace `ScanForLoadedHoleSceneAtStartup` 5s poll with `SceneManager.sceneLoaded` event subscription or direct `OnHoleLoaded(sceneName)` invocation from `GameplaySceneLoader` (matching the editor-side `PhysicsLabHolePicker` pattern) in Stage D or a cleanup stage; (d) close the queued `capture_core_frozen_time_fallback` ticket to remove the per-task screenshot bypass precedent.
