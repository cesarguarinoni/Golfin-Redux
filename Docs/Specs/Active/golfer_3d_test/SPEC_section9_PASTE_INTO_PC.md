## 9. Close-out scope — Architect review of commit `9c3da7e3d` (2026-09-07)

Reviewed `SITREP_ARCHITECT.md`, `HEARTBEAT.log`, `golfer_invariants.json` (35/1) and the three `evidence/` frames. The Address blocker is fixed and the fix is the right shape (stance derived from `BallState.Aiming`, competing triggers disarmed, render-checked assertions). Unblocked; **this section replaces the remaining work list.** Nothing else in §5–§8 is reopened.

> **Amended 2026-09-07 (Cesar, "1+2"): STOP all visual tuning.** The bend-at-the-waist, sliding legs and open hands are Unity-Humanoid retargeting Y Bot mocap onto a body of different proportions with no foot pinning — a data mismatch, not a presenter bug. 9.2, 9.3 and 9.4 are **cancelled**; only 9.1, 9.5, 9.6, 9.7 remain, plus the Architect-run check in 9.8. The test closes as **pipeline proven / visuals not**, and the visual answer moves to the roster-character spec.

| # | Item | Decision |
|---|---|---|
| 9.1 | `budget.tris` 15,632 > 15,000 | Remove the **Eyebrows** mesh from `PfGolfer_Test` (984 tris → 14,648). Do not touch the body or the club. Re-run the invariants. |
| 9.2 | ~~Swing never visible in play~~ | **CANCELLED 2026-09-07.** Left in the backlog as the production item. If already implemented, keep it only if the define-off path is byte-identical and all tests are green; otherwise revert. Was: Authorised, **inside `#if GOLFIN_GOLFER_TEST` only**: the ball launch is deferred to the measured impact time — Drive/Chip **1.167 s**, Putt **1.333 s** (IMPLEMENTER_REPORT §4) — so the existing cut-to-ball happens at impact, after the swing has been on screen. `GolferPresenter` receives `OnShotResolved`, starts the swing, and releases the launch at impact; with the define off the path is byte-identical to today. NOTE: find the seam where the resolved `ShotInput` reaches `BallStateMachine` / the physics launch and insert the deferral there — flag the exact method in the report. Bots (`BotSwing`) take the same delay. All parity / scheme EditMode tests must pass with the define **off**; with it on, `shot.swingPlays` must show the swing on the gameplay camera (capture the frame at t = 0.6 s after commit — no harness camera). This is the backlog item "ball launch delayed to impact frame" pulled forward for the test build only; the production version stays in `GPS_BACKLOG.md`. |
| 9.3 | ~~Grip~~ | **CANCELLED 2026-09-07 — no further work of any kind.** Was: **Stop tuning.** Seven rounds established the limit: Mixamo clips are empty-hand mocap; a real grip needs wrist roll the clips do not carry. Current numbers (all `grip.*` PASS) are the stand-in's final state. The real answer is club-in-hand clips (CMU 64 or Motion Cast #05) chosen with the roster models — backlog row added. `grip.wrapped_r` stays as is. |
| 9.4 | ~~Putter fingertip~~ | **CANCELLED 2026-09-07.** Was: Re-measure once on a green after 9.1–9.2; report the number. No tuning either way. |
| 9.5 | Golfer has no clothes | Out of scope for the test (Quaternius Standard ships underwear only). One line in the report. Roster models will have outfits. |
| 9.6 | §6 gate proof | **No iOS builds — Editor only (Cesar, 2026-09-07).** Gate proof is: EditMode sweep green with the define off; a new EditMode test asserting `GolferTestBuildGate` excludes `_Test` by default and includes it only when `IncludeTestAssets` is set (call the preprocessor's stash/restore directly, no player build); and `grep` showing every `GolferPresenter` / `GolferTestBootstrap` body is inside `#if GOLFIN_GOLFER_TEST`. The `punch it golfer` lane stays in the repo but is not exercised. Restore the active profile to **`iOS-Full-GPS`** before the final commit and say so. |
| 9.7 | Housekeeping | The tree carries uncommitted control-scheme edits from another session — do **not** sweep them. Verified no-op on the PC clone 2026-09-07. |

### 9.8 Mixamo-native retarget check (Architect, 2026-09-07) — the one experiment left
Hypothesis: the artefacts come from retargeting, not from the clips. Test it for $0: a **Mixamo character** with the same golf clips downloaded **"With Skin"**, so Mixamo retargets to a body it rigged itself and Unity does no retargeting at all (clip avatar = character avatar).

- Assets (Architect drops them in): `Assets/Art/3D/Characters/_Test/MixamoNative/` — `MixamoChar_TPose.fbx` (the character, T-pose, with skin + textures) and `ANIM_Golf_{Drive,Putt,DriveSetup}.fbx` + `ANIM_Idle.fbx` downloaded **on that character, With Skin, FBX for Unity, 30 fps, no reduction**.
- Code: import the character as Humanoid (Create From This Model); import the four clips as Humanoid with **Avatar → Copy From Other Avatar = the character's**; same root-motion bake settings as §5.1. Duplicate `PfGolfer_Test` → `PfGolfer_MixamoNative` swapping only the mesh + avatar; **no grip solver, no finger bake, no forearm aim — raw clips, club parented to the right hand at the socket only.** Same controller.
- Evidence: the same three frames as `evidence/` (address, at-rest, mid-swing at t = 0.6 s) from the same Hole 06 harness, side by side with the Quaternius ones, plus `stance.address.onGround` and the foot-slide distance during the swing for both prefabs. **No tuning either way.** The conclusion (retarget vs. clips) goes in the report §"Findings" and into `CHARACTER_3D_REMAKE_OPTIONS.md` as the pipeline decision for the roster models.

Exit (amended): invariants green except cancelled items, 9.8 side-by-side, gate EditMode test green, profile restored → `READY_FOR_SELF_REVIEW`.

