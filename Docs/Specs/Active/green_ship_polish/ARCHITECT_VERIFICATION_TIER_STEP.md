# Architect-side verification — tier-step-fix (Claude Code, 2026-06-01)

Independent data verification of the implementer's READY_FOR_ARCHITECT_REVIEW submission, done from the bake outputs directly (not a Unity re-import). Records the objective findings + the open call for Cesar.

## Verified (objective, reproducible)
1. **Change scope surgical.** `git diff HEAD Tools/GreenSlope/scripts/bake-green.mjs` is confined to `smoothRidgeBand()`: the `tierDrop` computation replaced (`hMax−hMin over all active` → `|mean(region0)−mean(region1)|`, two-pass far-from-ridge) + threading `tierStep` into the report INFO line. The `rampWidth` formula, smoothstep blend, continuity logic, Poisson loop, `classifyRegions`, non-tier early-return — untouched.
2. **Non-tier relH byte-identical.** For H5/H9/H13/H16 (sampled), `heightGridBase64` AND `slopeGridBase64` are byte-identical to HEAD. The ONLY field differing on the 14 non-tier holes is `sourceTag` ("green_slope_height_bake 2026-05-30" → "...2026-06-01"), a deterministic provenance string — which is why all 18 green.json show git-Modified. Spec Hard rule 3 (non-tier relH unchanged) is met; the "all 18 modified" is a cosmetic sourceTag bump, not a relH change.
3. **Tier holes: heightGrid changed, slopeGrid NOT.** H7/H11 `slopeGridBase64` byte-identical to HEAD; only `heightGridBase64` changed (the intended fix). rampWidth narrowed: H7 8.9→3.48 m, H11 9.6→4.96 m, H18 9.6→3.32 m.
4. **No staircase re-introduced (open item #4 = NO).** Cell-to-cell continuity vs HEAD: H7 over-gate cells (>5cm/cell ≡10% slope) DROP 40→8; H11 45→43; H18 20→21 — similar-or-better. The large single-cell deltas (H7 22.6 cm, H11 19.8 cm, H18 15.6 cm) are PRESENT AT HEAD (pre-existing ridge-endpoint mirrorFallback artifact + the real tier step) — not introduced by this fix. H7's HEAD max was 10.6 cm with MORE over-gate cells; the new localized step trades many moderate-slope cells for a few steeper ones at the step + endpoint.

## Open call for Cesar (the spec named Cesar's visual the real acceptance test)
- **H11:** clean two-tier (flat shelves + ~20cm step). Bimodal histogram. Unambiguous success.
- **H7 / H18:** ramp narrowed and step localized (real improvement over the pre-fix smear), BUT their tiers are themselves SLOPED → combined 1-D histogram stays unimodal (spec's objective bimodal gate met only for H11); global relH spread + plane-fit barely change (H7 0.474→0.474). The two regions DO sit at different mean heights (tierStep H7 18.5 cm, H18 17.7 cm), visible in region-labeled histograms + the localized step in cross-section. Result reads as "localized step between sloped tiers," NOT "two flat shelves." Preserving the authored tier slopes is arguably correct (real 2-tier greens have sloped tiers) — but whether H7 now READS as two-tier is Cesar's visual call.
- **H3 (open item #2):** tierStep ≈ 1.3 cm — the two authored regions are at essentially the same height. Not a real 2-tier in the data → likely an AUTHORING issue (ridge traced, but no height step between sides). The bake behaved correctly (tiny step → 1.0 m RidgeMinBand floor). Recommend: flag for authoring review, separate from this fix.
- **Ridge-endpoint artifact (open item #3):** pre-existing (HEAD had it, larger in aggregate), localized to ridge endpoints, from the pre-iter-13 mirrorFallback. Out of scope here. Recommend: separate backlog item if visible in-engine.

## Recommendation
The fix is surgical, safe, and does what it was scoped to do (un-smear the over-wide iter-13 band, localizing the tier step, cleanly restoring H11; preserving authored tier slopes; no non-tier change; no staircase regression). The corrected relH is the correct handoff for the seat/seam (B1) pass. The only open item is Cesar's visual judgement on whether H7's localized-step-on-sloped-tiers reads as the two-tier he wants — if YES, approve; if he wants more pronounced/flatter shelves, that exceeds the band fix (needs arrow re-authoring or a tier-flattening bake step) and is a new spec.
