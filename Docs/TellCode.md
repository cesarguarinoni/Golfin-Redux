# TellCode.md — legacy handoff channel (POINTER FILE)

> **This file is no longer the source of truth for current work.** It exists so that a session that starts by "checking TellCode" is redirected correctly instead of reading stale task blocks. (Before 2026-05-31 the body held a growing pile of superseded DONE/NEXT blocks with no current-state pointer — a session-start would land on month-old tasks and miss the live one. That failure mode is what this rewrite fixes.)

---

## ▶ CURRENT STATE — update this block at every session boundary

- **Active task:** `green_ship_polish` — green-fidelity ship-blockers. PASS 1 + PASS 2 seat/weld done & accepted; apron geometry accepted; ONE material fix remains, then ship.
- **PASS 1: tier-step-fix — ✅ DONE / Cesar-approved** (`SPEC_TIER_STEP_FIX.md`, commit `13fe08d6`). iter-13's `smoothRidgeBand` sized its ramp off whole-green relief not the tier STEP → H7 2-tier smeared to a slope. Fix: `tierDrop = |mean(region0)−mean(region1)|`. H7 ramp 8.9→3.48 m, staircase did not return, 14 non-tier holes byte-identical. H7 authored step is only **18.5 cm** (genuinely gentle — re-judged proud after PASS 2 seat; accepted).
- **PASS 2 seat + collar↔fairway weld — ✅ DONE / Cesar-accepted** (`SPEC_GREEN_SEAT_SEAM_B1.md`, `IMPLEMENTER_REPORT.md`). B1 terrain-following fitted-plane seat (least-squares through contour-vertex terrain samples): green reads proud not sunken, flag/cup on surface, 2-tier preserved, **relH-contribution delta = 0.000** (authored shape mathematically untouched). Collar↔fairway seam welded via **CDT-hole-constraint** (collar outer-ring polygon inserted as a constraint hole in the fairway CDT → coincident boundary verts by construction; green/fairway stay separate meshes). Cesar Q1 BLESSED this weld — DO NOT reopen/re-weld collar↔fairway. **B1 importer code committed `b05629ff`** (Lesson-AA risk closed).
- **Apron GEOMETRY — ✅ ACCEPTED** (`SPEC_GREEN_SEAT_TERRAIN_FRINGE.md`). Terrain-apron ring on the 2 terrain-bordered greens (H10, H18 — only greens with no fairway within collar range; other 16 fairway-welded-clean). Inner ring = `DilateContour(activeContour, GreenCollarWidth)` (coincident with collar outer ring by construction, weld gap 0), outer ring on terrain, laid over the raster `SetHoles` carve → **collar↔terrain sawtooth GONE**. Surface-classified `SurfaceType.Rough` (excluded from `BakedHeightProvider`, plays as rough). 16 fairway greens byte-identical (exactly 2 `GreenApron_1.mat` on disk). Root cause of the seam: raster hole edge (~1 m cells, holesResolution unset→2049 over ~2006 m) vs smooth collar polygon → T-junction teeth.
- **NEXT — terrain-apron MATERIAL — SPEC_READY** (`SPEC_TERRAIN_APRON_MATERIAL.md`). Cesar rejected the apron's **appearance**: textured with the fairway-fringe material (`T_Semirough_Albedo`, tile 6, no normal, smoothness 0.1, URP Lit) instead of the **rough terrain** it abuts → reads as a distinct dark band. **Architectural decision (verified in alphamap build L1332-1372): terrain is single-layer weight-1.0 rough, NOT a splat blend → single `T_Rough_*` material matches, NO custom shader.** Fix (3 changes, importer-only): (A) extend `CreateZoneMaterial` (L2352-2385) with optional normal+mask slot — existing callers byte-identical; (B) apron material = rough-layer replica (`T_Rough_Albedo` + `T_Rough_Normal` normalScale 0.4 + matte mask smoothness 0 + tile 8 + aniso 16); (C) `GreenTerrainApronWidth 1.5→1.1 m` (hard floor = raster cell 0.98 m; material fix is the real lever for "band too big"). **NEW acceptance gate the prior pass MISSED: apron INDISTINGUISHABLE from terrain at the grazing arc** (not just "no sawtooth" — the runs/row gates passed while the material was wrong; review-miss logged). H10+H18 re-shoot, apron not selected. Still must hold: sawtooth absent, proud rim graded, plays-as-rough, 16 fairway greens byte-identical.
- **Acceptance gate (terrain-apron material):** apron indistinguishable from surrounding terrain (albedo/tiling/normal/grazing-sheen) on H10+H18 grazing arc; geometry-accepted items unchanged; existing zone .mat byte-identical (Change A regression guard). Frame-extract native-res + LOOK before captioning (N=3 discipline; false-clean slipped twice).
- **History of failed attempts (do NOT repeat):** iter-14 adaptive-collar → MOUND+see-through (reverted). v1 perimeter-MIN flat seat → SUNKEN bowl (no flat scalar works on slope → B1's plane). v2 raised-pad → not chosen. collar↔fairway seam burned cut-polygon-overhang + vertex-snap + edge-projection → CDT-hole-constraint is the accepted one, no 4th collar↔fairway variation (Hard Rule 6). Terrain-apron is a DISTINCT seam (terrain side). Apron-material: prior gates checked sawtooth geometry only and passed while material was wrong — appearance gate added.
- **Live specs + evidence:** `Docs/Specs/Active/green_ship_polish/` — `SPEC_TERRAIN_APRON_MATERIAL.md` (NEXT, ready-to-kick), `ARCHITECT_HANDOFF_TERRAIN_APRON_MATERIAL.md` (Code's material diagnosis + code anchors), `SPEC_GREEN_SEAT_TERRAIN_FRINGE.md` (apron geometry — accepted), `ARCHITECT_HANDOFF_TERRAIN_SEAM.md` (per-green diag, scope=H10/18), `SPEC_GREEN_SEAT_SEAM_B1.md`+`IMPLEMENTER_REPORT.md` (PASS 2 done), `SPEC_TIER_STEP_FIX.md`+`ARCHITECT_VERIFICATION_TIER_STEP.md` (PASS 1 done), `screenshots/terrain_apron_h10_*` (band defect), `SPEC_ITER13.md` (queue authoritative).
- **Side deliverable — `green_orbit_videos` ✅ DONE / Cesar-approved** (in `Docs/Specs/Completed/green_orbit_videos/`). All 16 fairway-bordered greens verified clean (seam runs/row 0); new editor helper `GreenSlopeGridOrbit.cs`. 32 orbit clips reviewed ("All good"); videos LOCAL (gitignored, 544 MB, regenerable); docs+screenshots committed.
- **Last updated:** 2026-06-02 18:00 JST (Architect — apron geometry accepted; terrain-apron MATERIAL specced & ready to kick; single-rough-material decision verified in alphamap code).

---

## How current work is actually tracked now

1. **Live queue = `Docs/Specs/Active/`** — every pending/in-flight task is a folder there. That directory is authoritative for "what's open," not any inline list in this file.
2. **Per-task handoff = `Docs/Specs/Active/<slug>/HANDOFF.md`** (when present) — the next-session brief for that task.
3. **Completed tasks = `Docs/Specs/Completed/<slug>/`** — each closed task keeps its full record in its own folder.
4. **Session state headline = `Docs/AI_CONTEXT.md`** — upload at session start (project rule).
5. **Pre-2026-05-01 narrative history = `Docs/Archive/TELLCODE_HISTORY.md`.** The 2026-05 consolidated DONE/NEXT narrative that used to live in this file is preserved in **git history** — recover with `git show <pre-2026-05-31-commit>:Docs/TellCode.md` if ever needed.

## Rules (unchanged)

- Do **not** write new active tasks into this file. Specs go in per-task folders under `Docs/Specs/Active/<slug>/SPEC.md`.
- New UI tasks use the multi-agent pipeline at `.claude/agents/` (see `CLAUDE.md` § Multi-Agent Workflow).
- Live course importer is `HoleGeoImporter.cs` (NOT `HoleLiteImporter.cs` — deprecated, banner header, commit 980cc122). Verify via `grep MenuItem` before touching importer internals.
