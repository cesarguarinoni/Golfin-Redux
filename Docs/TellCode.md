# TellCode.md — legacy handoff channel (POINTER FILE)

> **This file is no longer the source of truth for current work.** It exists so that a session that starts by "checking TellCode" is redirected correctly instead of reading stale task blocks. (Before 2026-05-31 the body held a growing pile of superseded DONE/NEXT blocks with no current-state pointer — a session-start would land on month-old tasks and miss the live one. That failure mode is what this rewrite fixes.)

---

## ▶ CURRENT STATE — update this block at every session boundary

- **Shipped (do NOT re-report as pending):** `green_ship_polish` (Completed); **Loop v2 core** — A/B/C0/C1 + smoke-bot (Notion Orders 310–340), playable end-to-end since 2026-05-19, umbrella now in `Completed/loop_v2_scope/` (only Stage F animated-polish possibly-open — confirm before reopening); green authoring (`green_slope_authoring_tool` + `green_slope_height_bake`, Completed); **`ball_flight_trail`** — Code implemented (`4249c0da`) + closed to `Completed/ball_flight_trail/`.
- **Shipped since (do NOT re-report as pending):** `mode_select_system` (341), `practice_1v1_matchmaking_split` (342), `tap_feedback_fx`, **`1v1_ingame_ui` Phase 1** (343, `756ab280`), **`1v1_match_flow` Phase 2a** (344, `ec9ee885`), **`versus_bot_hardening`** (345, `4e700ae5`), and **`versus_bot_difficulty` Phase 2b** (Order 346, commit `5bee024c`, closed 2026-06-11 — level→error-band difficulty via `bot_difficulty.csv` (6 brackets, levels 1–240), post-decision injection with no safety re-check, H2-safe club noise via power re-inversion, putt noise suppression, `DebugLevelOverride`; dispersion proven lv1 ±5.5° vs lv180 ±0.38°; full pipeline incl. red-team PASS). All Completed.
- **NEXT / open:** **`water_splash_fx`** (Order 349, P2, Gameplay Polish) — SPEC WRITTEN 2026-06-12, SPEC_READY in `Docs/Specs/Active/water_splash_fx/SPEC.md` (**TELLCODE Tier 2** — additive presentation, mirrors `BallTrailController` pattern; bot video gate still required). Splash VFX + positional audio HOOK at water entry; trigger = `BallStateMachine.OnStateChanged` with `OBReason.Water` (falling-edge → fires at VISUAL arrival); spawn at `BallStateChange.Position`; pooled single `WaterSplash.prefab`, 2 intensity tiers, `PlaySFXAtPosition` hook with empty clip slot (asset → Order 350). Zero sim/state-machine edits — acceptance-gated. Awaiting Cesar kickoff. Other candidates still queued: `sound_effects` (350), `map_view_aiming` (352), physics follow-ups; `tree_aware_bot` (351) P3 — do not spec unprompted.
- **Shipped 2026-06-12 (do NOT re-report as pending):** **`tree_collisions`** (Order 348, FULL PIPELINE, closed to `Completed/tree_collisions/`, Notion Done) — deterministic tree collision baked into the sim: 3-source bake → per-hole `tree_obstacles.csv` + per-prefab `tree_collision_profiles.csv` → `TreeObstacleProvider` XZ grid → segment-vs-cylinder in all phases. Trunk = hard reflect (restitution 0.15) incl. roll/putt and the iter-7 `frac=0` containment (stuck-ball fix); canopy = ONE-TIME entry-crossing impulse `canopyHitDamping=0.40` (D3 v1 per-step damping rejected by Cesar — slow-mo bug; iter-6 "8/8" was a stuck-ball false pass, adjudicated in `Completed/tree_collisions/FINDINGS_iter7_canopy_test.md`). Auto re-bake on `Hole_NN_Geo` save via staleness hash; absent CSV = bit-exact Phase-6. Code: WIP checkpoint `2fb4c2b7` + iter-8 `4a2813a1` (9/9 tests, gate video). Feel-tuning lever: `tree_collision_profiles.csv` per prefab, no code.
- **Also queued:** physics audit follow-ups (`strength_velocity_short_game_scaling`, `club_control_aim_arrow_speed`, `ball_rebound_perceptibility`, `ball_roll_coefficient_retune`, `character_recovery_stamina_regen`), `physics_lab_controller_rename` (P3), `phone_build_smoke_test` (Order 420, blocked on Ken's dev-account issue). Card-editability specs are non-gated and can run ahead. **347 `1v1_result_rewards_display` (P3, do not spec unprompted):** Figma refs logged 2026-06-12 — result screens = nodes `13274-877` + `13275-2628` (file `5gEAHjl6xAtW8iYY7NMvWd`); Cesar sequencing: screens appear ONLY AFTER the WIN/LOSE/DRAW banner plays. Full links in Notion 347. **NEW 2026-06-11 (Cesar):** `tree_collisions` (348), `water_splash_fx` (349), `sound_effects` (350) — all P2 Queued, Phase "Gameplay Polish", Notion-only (no specs yet). **NEW 2026-06-12 (Cesar):** `map_view_aiming` (352, P2 Queued) — tap the Shot UI hole-map (`HoleCardWidget`) → full-screen top-down map view with aiming; Notion-only, no spec yet. **NEW 2026-06-12 (Cesar):** `localization_audit` (353, P2 Queued) — system + CSV exist (`Assets/Localization/`, `LocalizedText` component) but post-wiring UI never got the component; audit tool (scan scenes+prefabs for unlocalized TMP/Text, report = demoable) → batch-add via existing `LocalizationEditorHelper` → fill JP+EN CSV rows; Notion-only, no spec yet.
- **Last updated:** 2026-06-18 (Architect — **`map_view_aiming` (Order 352) SPEC_READY ✅**, now in `Docs/Specs/Active/map_view_aiming/` (kickoff `Use the implementer subagent on "map_view_aiming"`). FULL PIPELINE Tier-3: tap Shot-UI hole-map → full-screen **hero-angle** (NOT ortho — kickoff overridden) live RT render of the real hole; drag+tap aim writes back via aim heading; pinch-zoom+pan; aim-only; map-mode hides all Shot-UI chrome except the club button (relabel **SHOOT** = close); markers = ball/flag/draggable landing zone/mocked guide line (bends w/ Fade-Draw, reuses `AimLineBendRenderer.LateralAtT`) + power rings @80/100/120% of `_maxCarryYards`. Notion 352 → In Progress. **The `water_splash_fx` NEXT bullet above + the "map_view_aiming Notion-only, no spec" notes are now STALE** — 349/350/354/355/356 all shipped; see AI_CONTEXT headline. **NEXT after 352 = Cesar's pick** (queued physics-audit follow-ups, `localization_audit` 353).)

---

## How current work is actually tracked now

1. **Live queue = `Docs/Specs/Active/`** — every pending/in-flight task is a folder there. That directory is authoritative for "what's open," not any inline list in this file.
2. **Per-task handoff = `Docs/Specs/Active/<slug>/HANDOFF.md`** (when present) — the next-session brief for that task.
3. **Completed tasks = `Docs/Specs/Completed/<slug>/`** — each closed task keeps its full record in its own folder.
4. **Session state headline = `Docs/AI_CONTEXT.md`** — upload at session start (project rule).
5. **Pre-2026-05-01 narrative history = `Docs/Archive/TELLCODE_HISTORY.md`.** The 2026-05 consolidated DONE/NEXT narrative that used to live in this file is preserved in **git history** — recover with `git show <pre-2026-05-31-commit>:Docs/TellCode.md` if ever needed.

## Rules (unchanged)

- Do **not** write new active tasks into this file. Specs go in per-task folders under `Docs/Specs/Active/<slug>/SPEC.md`.
- New UI tasks use the multi-agent pipeline at `.claude/agents/` (see `CLAUDE.md` § Multi-Agent Workflow).
- Live course importer is `HoleGeoImporter.cs` (NOT `HoleLiteImporter.cs` — deprecated, banner header, commit 980cc122). Verify via `grep MenuItem` before touching importer internals.
