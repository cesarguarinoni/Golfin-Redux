# TellCode.md — legacy handoff channel (POINTER FILE)

> **This file is no longer the source of truth for current work.** It exists so that a session that starts by "checking TellCode" is redirected correctly instead of reading stale task blocks. (Before 2026-05-31 the body held a growing pile of superseded DONE/NEXT blocks with no current-state pointer — a session-start would land on month-old tasks and miss the live one. That failure mode is what this rewrite fixes.)

---

## ▶ CURRENT STATE — update this block at every session boundary

- **Active task:** `green_ship_polish` — green-fidelity ship-blockers. PASS 1 + PASS 2 seat/weld done & accepted; ONE seam sub-fix remains (terrain-apron), then ship.
- **PASS 1: tier-step-fix — ✅ DONE / Cesar-approved** (`SPEC_TIER_STEP_FIX.md`, commit `13fe08d6`). iter-13's `smoothRidgeBand` sized its ramp off whole-green relief not the tier STEP → H7 2-tier smeared to a slope. Fix: `tierDrop = |mean(region0)−mean(region1)|`. H7 ramp 8.9→3.48 m, staircase did not return, 14 non-tier holes byte-identical. H7 authored step is only **18.5 cm** (genuinely gentle — re-judged proud after PASS 2 seat; accepted).
- **PASS 2 seat + collar↔fairway weld — ✅ DONE / Cesar-accepted** (`SPEC_GREEN_SEAT_SEAM_B1.md`, `IMPLEMENTER_REPORT.md`). B1 terrain-following fitted-plane seat (least-squares through contour-vertex terrain samples): green reads proud not sunken, flag/cup on surface, 2-tier preserved, **relH-contribution delta = 0.000** (authored shape mathematically untouched). Collar↔fairway seam welded via **CDT-hole-constraint** (collar outer-ring polygon inserted as a constraint hole in the fairway CDT → coincident boundary verts by construction; green/fairway stay separate meshes). Cesar Q1 BLESSED this weld — DO NOT reopen/re-weld collar↔fairway. Gates #1,2,3,5,6 PASS. **B1 importer code now committed `b05629ff`** (2026-06-01 — was the long-uncommitted live importer; Lesson-AA risk closed).
- **NEXT — terrain-apron — SPEC_READY** (`SPEC_GREEN_SEAT_TERRAIN_FRINGE.md`). The LAST ship-blocker: collar↔**terrain** sawtooth on greens with no bordering fairway. Root cause: terrain hole is a rasterized `SetHoles` mask (holesResolution unset → defaults 2049 over ~2006 m → ~1 m cells) → stair-stepped edge vs smooth collar polygon → ~1 m T-junction teeth. **Scope = ONLY H10 + H18** (Cesar-confirmed; the only 2 greens with no fairway within collar range; other 16 are fairway-welded-clean). Terrain intrudes H10 ~0.19 m (proud rim + teeth), H18 ~0.075 m (flat teeth) — carve is genuinely needed on both, so it stays; we hide its edge. **Fix = terrain apron ring**, terrain-bordered greens only: inner ring = `DilateContour(activeContour, GreenCollarWidth)` (= collar outer ring by construction → coincident weld, no Hard-Rule-6 risk, NOT a collar↔fairway change), outer ring = `+GreenTerrainApronWidth (1.5 m)` on terrain; **terrain/rough material + own surface type** (plays as rough, green apparent size unchanged); laid OVER the raster hole so the visible edge is mesh-on-terrain not mesh-meets-raster; grades H10's 0.19 m proud rim as a gentle ramp. Importer-only, no bake/schema/physics-gate change (apron is rough, excluded from green height provider).
- **Acceptance gate (terrain-apron):** H10+H18 no sawtooth (runs/row ≤ 3 at a REALISTIC lighter-pixel threshold, NOT pure-white — false-clean slipped twice), H10 proud rim graded not standing, apron reads+plays as rough, **16 fairway greens byte-identical** (H7 weld unchanged). H10 not yet visually inspected → shoot before/after FIRST (likely worse than H18). Frame-extract orbit at native res + LOOK before captioning (N=3 discipline).
- **History of failed attempts (do NOT repeat):** iter-14 adaptive-collar → MOUND+see-through (reverted). v1 perimeter-MIN flat seat → SUNKEN bowl (no flat scalar works on slope → B1's plane). v2 raised-pad → not chosen. collar↔fairway seam has burned cut-polygon-overhang + vertex-snap + edge-projection → CDT-hole-constraint is the accepted one, no 4th collar↔fairway variation (Hard Rule 6). Terrain-apron is a DISTINCT seam (terrain side), not a 4th fairway attempt.
- **Live specs + evidence:** `Docs/Specs/Active/green_ship_polish/` — `SPEC_GREEN_SEAT_TERRAIN_FRINGE.md` (NEXT, ready-to-kick), `ARCHITECT_HANDOFF_TERRAIN_SEAM.md` (Code's diagnosis + per-green diag, Cesar-amended scope=H10/18), `SPEC_GREEN_SEAT_SEAM_B1.md`+`IMPLEMENTER_REPORT.md`+`SELF_REVIEW.md` (PASS 2 done), `SPEC_TIER_STEP_FIX.md`+`ARCHITECT_VERIFICATION_TIER_STEP.md` (PASS 1 done), `screenshots/b1_merged_h18_t7s.png` (teeth) vs `b1_merged_h07_canonical_sw.png` (clean welded), v1/v2 superseded specs, `SPEC_ITER13.md` (queue authoritative).
- **Side deliverable — `green_orbit_videos` ✅ DONE / Cesar-approved** (moved to `Docs/Specs/Completed/green_orbit_videos/`). Verified all 16 fairway-bordered greens (1-9,11-17) clean like H7 (seam runs/row 0); new editor helper `GreenSlopeGridOrbit.cs` renders the full-green `PutterGreenGrid` slope grid in a `Hole_NN_Geo` scene (non-destructive `HideFlags.DontSave`, tree-safe green-collider raycast). 32 orbit clips (normal + slope-grid, with trees) reviewed in-folder ("All good"). Videos kept LOCAL (gitignored, 544 MB, regenerable); docs+screenshots committed.
- **Last updated:** 2026-06-01 (Code — green_orbit_videos DONE + B1 importer code committed `b05629ff`; green_ship_polish terrain-apron H10/H18 still NEXT / ready-to-kick).

---

## How current work is actually tracked now

1. **Live queue = `Docs/Specs/Active/`** — every pending/in-flight task is a folder there. That directory is authoritative for "what's open," not any inline list in this file.
2. **Per-task handoff = `Docs/Specs/Active/<slug>/HANDOFF.md`** (when present) — the next-session brief for that task.
3. **Completed tasks = `Docs/Specs/Completed/<slug>/`** — each closed task keeps its full record in its own folder.
4. **Session state headline = `Docs/AI_CONTEXT.md`** — upload at session start (project rule).
5. **Pre-2026-05-01 narrative history = `Docs/Archive/TELLCODE_HISTORY.md`.** The 2026-05 consolidated DONE/NEXT narrative that used to live in this file is preserved in **git history** — recover with `git show <pre-2026-05-31-commit>:Docs/TellCode.md` if ever needed.

## Rules (unchanged)

- Do **not** write new active tasks into this file. Specs go in per-task folders under `Docs/Specs/Active/<slug>/SPEC.md`.
- New UI tasks use the multi-agent pipeline at `.claude/agents/` (see `CLAUDE.md` § Multi-Agent Workflow).
- Live course importer is `HoleGeoImporter.cs` (NOT `HoleLiteImporter.cs` — deprecated, banner header, commit 980cc122). Verify via `grep MenuItem` before touching importer internals.
